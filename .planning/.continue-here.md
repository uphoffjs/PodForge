---
phase: pre-phase (Stryker mutation fixes)
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-02-24T17:03:42.141Z
---

<current_state>
Fixing 34 Stryker mutation survivors to reach near-100% score before continuing v2.0 milestone setup. Tests written for EventPage.tsx (24 survivors) and LandingPage.tsx (1 survivor) — all 62 tests pass. Still need to handle PlayerList.tsx (3 survivors) and 6 single-survivor files, then run Stryker to verify.
</current_state>

<completed_work>

- Task 1: Fix EventPage.tsx Stryker survivors (24 mutants) — IN PROGRESS
  - Added hoisted mocks for useEventChannel, useVisibilityRefetch, useDropPlayer (trackable args)
  - Added test: hooks receive '' when eventId undefined (kills ?? '' string fallback mutations)
  - Added test: justJoinedRef prevents clearing when player not in list (kills BlockStatement, ConditionalExpression)
  - Added test: justJoinedRef blocks across multiple renders while pending (kills if(playerExists)→if(true))
  - Added test: justJoinedRef resets after confirmation, then clearing works (kills BooleanLiteral, some→every)
  - Added test: stored player not cleared among multiple players (kills some→every on verification line 71)
  - Added test: re-checks localStorage when eventId changes (kills dep array [eventId]→[] mutation)
  - Added test: handleJoined uses current eventId after change (kills dep array mutation)
  - Added test: handleCopyLink uses current eventId after change (kills dep array mutation)
  - Added test: no timer when no new players on rerender (kills added.size > 0 → >= 0)
  - Added test: cleanup clears highlight timer on unmount (kills cleanup → undefined mutation)
  - ALL 62 TESTS PASS (verified with vitest run)
- Task 5: Fix LandingPage.tsx survivor — DONE
  - Added test: form submit with empty input via fireEvent.submit (kills if(eventId)→if(true))
  - Test passes
</completed_work>

<remaining_work>

- Task 1: EventPage.tsx — need to run Stryker to verify how many of the 24 are actually killed
- Task 2: Fix PlayerList.tsx survivors (3 mutants) — not started, need Stryker output to identify exact mutations
- Task 3: Fix remaining 6 single-survivor files — not started:
  - useEventChannel.ts (1 survivor)
  - useJoinEvent.ts (1 survivor)
  - useVisibilityRefetch.ts (1 survivor)
  - useAddPlayer.ts (1 survivor)
  - AddPlayerForm.tsx (1 survivor)
  - JoinEventForm.tsx (1 survivor)
- Task 4: Run full Stryker to verify all survivors killed — blocked on tasks 1-3
- After Stryker: Continue v2.0 milestone setup (REQUIREMENTS.md, roadmap commit)
</remaining_work>

<decisions_made>

- Used hoisted mocks for useEventChannel/useVisibilityRefetch/useDropPlayer to enable argument verification
- Used vi.getTimerCount() to test timer creation/cleanup without complex timing scenarios
- Used rerender pattern with mock changes to test useEffect dependency arrays
- Used fireEvent.submit to bypass disabled button for LandingPage empty-input test
</decisions_made>

<blockers>
- None — just need more context window to continue
</blockers>

<context>
Strategy: Write targeted tests based on PAUSE-CONTEXT.md analysis of the 34 survivors, then verify with Stryker. The EventPage tests target specific Stryker mutation categories (justJoinedRef guard, dependency arrays, string fallbacks, highlight detection). For the remaining files (PlayerList + 6 single-survivor hooks/components), the exact mutations are unknown — need to run Stryker on those files individually to identify what survives before writing fixes.

The PAUSE-CONTEXT.md from the previous session has detailed analysis of all 34 survivors categorized by file and type. Reference it heavily.

Modified test files (untracked, need to be committed):
- src/pages/EventPage.test.tsx (major additions: 11 new tests)
- src/pages/LandingPage.test.tsx (1 new test + fireEvent import)
</context>

<next_action>
1. Run Stryker on EventPage.tsx specifically: `npx stryker run --mutate "src/pages/EventPage.tsx"`
2. Check how many of the 24 survivors are now killed
3. Fix any remaining EventPage survivors
4. Run Stryker on PlayerList.tsx to identify exact 3 survivors: `npx stryker run --mutate "src/components/PlayerList.tsx"`
5. Fix PlayerList survivors
6. Run Stryker on each single-survivor file to identify and fix
7. Full Stryker run to verify near-100%
8. Commit all test fixes
9. Continue v2.0 milestone (REQUIREMENTS.md)
</next_action>
