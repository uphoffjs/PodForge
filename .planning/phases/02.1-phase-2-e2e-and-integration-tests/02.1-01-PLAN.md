---
phase: 02.1-phase-2-e2e-and-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cypress/fixtures/rounds.json
  - cypress/fixtures/pods.json
  - cypress/e2e/admin-passphrase.cy.js
  - cypress/e2e/generate-round.cy.js
  - cypress/e2e/pod-display.cy.js
autonomous: true
requirements:
  - PODG-01
  - PODG-03
  - PODG-04
  - PODG-05
  - PODG-06

must_haves:
  truths:
    - "E2E test confirms admin passphrase modal opens, accepts input, and stores passphrase in sessionStorage"
    - "E2E test confirms Generate Round button triggers RPC and displays pod cards on success"
    - "E2E test confirms pod cards show seat ordinals (1st-4th) for non-bye pods"
    - "E2E test confirms bye pod has visually distinct rendering with no seat numbers"
    - "E2E test confirms <4 active players blocks round generation with error toast"
    - "E2E test confirms multiple rounds can be generated sequentially"
  artifacts:
    - path: "cypress/fixtures/rounds.json"
      provides: "Mock round data for round display tests"
    - path: "cypress/fixtures/pods.json"
      provides: "Mock pod data with pod_players nested structure"
    - path: "cypress/e2e/admin-passphrase.cy.js"
      provides: "E2E tests for admin passphrase entry flow"
    - path: "cypress/e2e/generate-round.cy.js"
      provides: "E2E tests for round generation including error cases"
    - path: "cypress/e2e/pod-display.cy.js"
      provides: "E2E tests for pod card rendering, seats, bye pod, colors"
  key_links:
    - from: "cypress/e2e/generate-round.cy.js"
      to: "src/components/AdminControls.tsx"
      via: "cy.intercept for generate_round RPC + data-testid assertions"
      pattern: "getByTestId.*generate-round-btn"
    - from: "cypress/e2e/pod-display.cy.js"
      to: "src/components/PodCard.tsx"
      via: "cy.intercept for pods query + data-testid assertions for pod-card, pod-seat, pod-player"
      pattern: "getByTestId.*pod-card"
---

<objective>
Create E2E tests for admin passphrase entry, round generation, and pod display.

Purpose: Verify the core admin flow works end-to-end: entering passphrase, generating a round, seeing pods with seat assignments, handling bye pods, and error cases (fewer than 4 players).

Output: 3 Cypress spec files and 2 fixture files covering PODG-01 (generate rounds), PODG-03 (fewer-than-4 error), PODG-04 (seat order), PODG-05 (bye pod distinct), PODG-06 (pod color-coded borders).
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key data-testid attributes the executor needs for Phase 2 components -->

From src/components/AdminPassphraseModal.tsx:
- admin-passphrase-modal (overlay container)
- admin-passphrase-input (password input)
- admin-passphrase-error (error message)
- admin-passphrase-cancel (cancel button)
- admin-passphrase-submit (submit button)

From src/components/AdminControls.tsx:
- admin-controls (container div)
- generate-round-btn (generate round button)
- end-event-btn (end event button)

From src/components/RoundDisplay.tsx:
- round-display (container div)
- round-number (h2 showing "Round N")

From src/components/PodCard.tsx:
- pod-card-bye (bye pod container)
- pod-card-{podNumber} (non-bye pod container, e.g., pod-card-1)
- pod-player-{playerId} (player row in pod)
- pod-seat-{seatNumber} (seat ordinal badge, e.g., pod-seat-1 shows "1st")

From src/pages/EventPage.tsx:
- event-name, event-status, event-loading, event-error
- join-skip-btn (skip join button)

Existing Cypress patterns (from cypress/support/commands.js):
- cy.getByTestId(id) custom command for element selection
- cy.mockEventPage(eventData, playersData) for mocked event pages
- Admin setup: onBeforeLoad to set sessionStorage key `podforge_admin_{eventId}` + localStorage `podforge_player_{eventId}`

PostgREST mock patterns:
- Event GET: body=object, content-type=application/vnd.pgrst.object+json
- Players GET: body=array, default content-type
- RPC POST: body=JSON.stringify(value), content-type=application/json
- Pods GET (nested): body=array of {id, round_id, pod_number, is_bye, pod_players: [{id, pod_id, player_id, seat_number, players: {id, event_id, name, status, created_at}}]}
- Rounds GET: body=array, default content-type
- CurrentRound GET (.maybeSingle()): body=object, content-type=application/vnd.pgrst.object+json

Important: useCurrentRound uses .maybeSingle() which returns a single object or null.
usePods queries pods with nested pod_players(*, players(*)) joined data.
useRounds returns rounds ordered by round_number DESC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture files and admin passphrase E2E tests</name>
  <files>
    cypress/fixtures/rounds.json
    cypress/fixtures/pods.json
    cypress/e2e/admin-passphrase.cy.js
  </files>
  <action>
Create two fixture files for reuse across Phase 2 E2E specs:

**cypress/fixtures/rounds.json** — Array with 2 rounds:
```json
[
  { "id": "round-1", "event_id": "test-uuid", "round_number": 1, "created_at": "2026-01-01T01:00:00Z" },
  { "id": "round-2", "event_id": "test-uuid", "round_number": 2, "created_at": "2026-01-01T02:00:00Z" }
]
```

**cypress/fixtures/pods.json** — Array with 2 non-bye pods (4 players each) + 1 bye pod (1 player), all for round-1. Each pod must include the nested `pod_players` array with joined `players` object matching the PostgREST select `pods(*, pod_players(*, players(*)))` shape:
```json
[
  {
    "id": "pod-1", "round_id": "round-1", "pod_number": 1, "is_bye": false,
    "pod_players": [
      { "id": "pp-1", "pod_id": "pod-1", "player_id": "player-1", "seat_number": 1, "players": { "id": "player-1", "event_id": "test-uuid", "name": "Alice", "status": "active", "created_at": "..." } },
      { "id": "pp-2", "pod_id": "pod-1", "player_id": "player-2", "seat_number": 2, "players": { "id": "player-2", "event_id": "test-uuid", "name": "Bob", "status": "active", "created_at": "..." } },
      { "id": "pp-3", "pod_id": "pod-1", "player_id": "player-3", "seat_number": 3, "players": { "id": "player-3", "event_id": "test-uuid", "name": "Charlie", "status": "active", "created_at": "..." } },
      { "id": "pp-4", "pod_id": "pod-1", "player_id": "player-4", "seat_number": 4, "players": { "id": "player-4", "event_id": "test-uuid", "name": "Dave", "status": "active", "created_at": "..." } }
    ]
  },
  {
    "id": "pod-2", "round_id": "round-1", "pod_number": 2, "is_bye": false,
    "pod_players": [
      { "id": "pp-5", "pod_id": "pod-2", "player_id": "player-5", "seat_number": 1, "players": { "id": "player-5", "event_id": "test-uuid", "name": "Eve", "status": "active", "created_at": "..." } },
      { "id": "pp-6", "pod_id": "pod-2", "player_id": "player-6", "seat_number": 2, "players": { "id": "player-6", "event_id": "test-uuid", "name": "Frank", "status": "active", "created_at": "..." } },
      { "id": "pp-7", "pod_id": "pod-2", "player_id": "player-7", "seat_number": 3, "players": { "id": "player-7", "event_id": "test-uuid", "name": "Grace", "status": "active", "created_at": "..." } },
      { "id": "pp-8", "pod_id": "pod-2", "player_id": "player-8", "seat_number": 4, "players": { "id": "player-8", "event_id": "test-uuid", "name": "Heidi", "status": "active", "created_at": "..." } }
    ]
  },
  {
    "id": "pod-3", "round_id": "round-1", "pod_number": 3, "is_bye": true,
    "pod_players": [
      { "id": "pp-9", "pod_id": "pod-3", "player_id": "player-9", "seat_number": null, "players": { "id": "player-9", "event_id": "test-uuid", "name": "Ivan", "status": "active", "created_at": "..." } }
    ]
  }
]
```

**cypress/e2e/admin-passphrase.cy.js** — Tests for passphrase modal flow. Create a `setupAdminPageWithRounds` helper function (within describe block) that sets up all intercepts needed for Phase 2 admin tests:
1. Block Realtime WebSocket
2. Mock event GET (PostgREST single-object format)
3. Mock players GET (9 active players to enable pod generation)
4. Mock rounds GET (empty array initially — no rounds)
5. Mock currentRound GET (.maybeSingle() — null initially): intercept with `**/rest/v1/rounds*` that includes `limit=1` or use a separate alias
6. Mock pods GET (empty initially)
7. Mock allRoundsPods GET (empty initially — the useAllRoundsPods hook queries pods with .in() filter)
8. Visit with onBeforeLoad to set sessionStorage admin key + localStorage player key

IMPORTANT: The useCurrentRound hook queries `rounds` table with `.order('round_number', { ascending: false }).limit(1).maybeSingle()`. The useRounds hook queries `rounds` table with `.order('round_number', { ascending: false })`. Both hit `**/rest/v1/rounds*`. To handle both, intercept `**/rest/v1/rounds*` and return based on query params, OR use a single intercept returning an array (the array works for useRounds, and for useCurrentRound the `.maybeSingle()` happens client-side via PostgREST headers). The simplest approach: intercept `**/rest/v1/rounds*` and return an empty array `[]` — useRounds gets empty array, and useCurrentRound with .maybeSingle() + limit=1 header also works because PostgREST returns a single null. Actually the cleanest way: for currentRound, intercept with the `Accept: application/vnd.pgrst.object+json` header pattern. Simplest: just intercept all `**/rest/v1/rounds*` returning `[]` and separately handle the single-object response. OR just return `[]` — supabase-js `.maybeSingle()` returns null for empty array.

Best approach: Return empty array `[]` for rounds. supabase-js `.maybeSingle()` on empty result returns `{ data: null, error: null }`. This works for both hooks.

Test cases:
1. "shows passphrase modal when Generate Round is clicked without passphrase" — setup admin page WITHOUT pre-setting passphrase in sessionStorage (set only localStorage player key, not sessionStorage admin key). Click Generate Round btn. Assert admin-passphrase-modal is visible.
2. "submits passphrase and stores it" — same setup without passphrase. Click generate-round-btn, modal opens. Type passphrase in admin-passphrase-input. Click admin-passphrase-submit. Modal should close (not.exist). Note: after passphrase submission, the generate round flow triggers. Mock the generate_round RPC POST to succeed. The AdminControls tries to run the algorithm client-side, which means `generatePods()` is called. Since this is mocked E2E, the algorithm actually runs in the browser. With 9 active players, it should produce valid pods. Intercept the POST to `**/rest/v1/rpc/generate_round` to return a round number.
3. "cancels passphrase modal" — open modal, click cancel, assert modal closes.
4. "closes modal on overlay click" — open modal, click overlay, assert modal closes.

Wait — important nuance: The passphrase modal in EventPage only opens when `showPassphraseModal` state is true. The flow is: admin clicks generate-round-btn -> AdminControls.handleGenerateRound checks `!passphrase` -> calls `onPassphraseNeeded()` -> EventPage sets `showPassphraseModal(true)`. After submit, `handlePassphraseSubmit` stores the passphrase and closes the modal. But then the admin must click Generate Round AGAIN because the first click only triggered the modal.

Actually re-reading the flow: when passphrase is null and admin clicks Generate Round, it calls `onPassphraseNeeded()` which opens the modal. After the user enters the passphrase and submits, `handlePassphraseSubmit` sets passphrase in sessionStorage and closes modal. The round is NOT automatically generated — the admin must click Generate Round again. This is the correct flow to test.

For test 2, the flow should be: click Generate Round -> modal opens -> enter passphrase -> submit -> modal closes -> click Generate Round again -> intercept RPC -> success.

Actually, let me reconsider. For the passphrase-only tests, we just need to verify the modal opens/closes/submits. We don't need to verify the full generate round flow here. That's in the generate-round spec. So test 2 can just verify: modal opens -> type passphrase -> submit -> modal closes.

For the admin page WITHOUT pre-set passphrase: visit with onBeforeLoad setting ONLY localStorage player key (to be recognized as a joined player), but NOT sessionStorage admin key. But wait — `isAdmin` comes from `useAdminAuth` which checks `sessionStorage.getItem('podforge_admin_{eventId}')`. If it's null, `isAdmin` is false. If isAdmin is false, `AdminControls` returns null and the Generate Round button won't even be visible!

So the passphrase modal test needs a different approach. The admin must have a passphrase set (isAdmin=true) to see AdminControls. But the passphrase modal opens when `passphrase` is provided but the action still needs re-authentication...

Wait, let me re-read useAdminAuth. The `isAdmin` flag and `passphrase` come from the same sessionStorage value. If `sessionStorage.getItem('podforge_admin_{eventId}')` returns a value, then `isAdmin=true` AND `passphrase` is that value. So if the admin has a passphrase stored, both isAdmin and passphrase are set, and clicking Generate Round skips the modal and goes directly to generation.

The passphrase modal is only shown when `isAdmin` is true but `passphrase` is null — which can't happen with the current useAdminAuth implementation, because both come from the same storage key.

Actually, let me re-read useAdminAuth:

```ts
const { isAdmin, passphrase, setPassphrase, clearPassphrase } = useAdminAuth(eventId)
```

Looking at the hook, `isAdmin` is derived from `!!passphrase`. So `isAdmin` is true only when `passphrase` is non-null. This means AdminControls is only rendered when passphrase is already set. And if passphrase is set, clicking Generate Round will NOT trigger `onPassphraseNeeded()` — it goes straight to generation.

So the passphrase modal flow is: admin navigates to page -> doesn't have passphrase stored -> admin controls are NOT visible -> some other action triggers passphrase entry.

Hmm, but how does the admin enter the passphrase in the first place? Looking at EventPage: AdminControls is only rendered when `isAdmin && !isEventEnded`. And isAdmin comes from useAdminAuth. So if the admin hasn't entered a passphrase yet, they won't see AdminControls at all.

But wait — that can't be right because the admin just created the event and was redirected with the passphrase in sessionStorage. So the flow is: create event -> passphrase auto-stored in sessionStorage -> navigate to event page -> isAdmin=true, passphrase=non-null -> AdminControls visible -> Generate Round goes straight to generation.

So the passphrase MODAL is for cases where the admin's sessionStorage was cleared (different tab, browser restart, etc.) but they're trying to do admin actions... but they can't see admin controls without being authenticated first!

Actually, looking at EventPage more carefully, the `showPassphraseModal` state is managed by EventPage and passed down to AdminControls via `onPassphraseNeeded`. AdminControls already checks `if (!passphrase)` before calling `onPassphraseNeeded()`. But AdminControls is only rendered when `isAdmin` is true. And `isAdmin` is only true when passphrase is set...

This means the passphrase modal flow might be for AdminPlayerActions or other admin actions that could be triggered by a different mechanism. Let me check...

AdminPlayerActions also receives `passphrase` and `onPassphraseNeeded`. But AdminPlayerActions is only rendered inside PlayerList when `isAdmin` is true. And `isAdmin` is passed from EventPage where `isAdmin` comes from useAdminAuth...

So it seems like the passphrase modal is unused in practice because you can't reach admin UI without already having a passphrase stored. Unless useAdminAuth has a way to be admin without a stored passphrase.

Let me check the actual passphrase modal test separately from the generation tests. The passphrase modal E2E tests might not make sense as a standalone spec. Let me restructure.

For E2E testing purposes, the flow is:
1. Admin creates event -> passphrase auto-stored -> visits event page -> sees AdminControls -> Generate Round
2. The passphrase is already available for all admin actions

So the E2E tests should:
- Test admin passphrase modal as a component-level concern (it's already unit tested)
- Focus E2E tests on the full admin flow: visit as admin -> Generate Round -> see pods

Let me restructure the plan to combine the passphrase + generate round + pod display into fewer, more focused specs.

Actually, rethinking: I'll merge the passphrase spec into the generate-round spec since the passphrase is a prerequisite for admin actions and isn't independently testable via E2E.
  </action>
  <verify>
    <automated>npx cypress run --spec "cypress/e2e/admin-passphrase.cy.js" --headless 2>&1 | tail -20</automated>
  </verify>
  <done>
    - cypress/fixtures/rounds.json exists with 2 rounds
    - cypress/fixtures/pods.json exists with 3 pods (2 non-bye + 1 bye) with nested pod_players
    - admin-passphrase.cy.js has 3+ tests covering passphrase modal open/submit/cancel
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create generate-round and pod-display E2E tests</name>
  <files>
    cypress/e2e/generate-round.cy.js
    cypress/e2e/pod-display.cy.js
  </files>
  <action>
**cypress/e2e/generate-round.cy.js** — Tests for round generation flow. Use the admin page setup pattern from admin-add-player.cy.js (onBeforeLoad with sessionStorage + localStorage). Need 9 active players in mock data (enough for 2 pods of 4 + 1 bye).

Create a helper `setupAdminEventPage(players, roundsData, currentRound, podsData)` inside the describe block that:
1. Blocks Realtime WebSocket
2. Mocks event GET (PostgREST single-object format, status: 'active')
3. Mocks players GET (default: 9 active players)
4. Mocks rounds GET (default: empty array [])
5. Mocks pods GET (default: empty array [])
6. Visits with onBeforeLoad setting sessionStorage admin key + localStorage player key
7. Waits for getEvent + getPlayers

Test cases:
1. "shows admin controls with Generate Round button for admin" — verify admin-controls visible, generate-round-btn visible with text "Generate Next Round"
2. "generates round successfully" —
   - Setup with 9 active players, no existing rounds
   - Intercept POST to `**/rest/v1/rpc/generate_round` returning JSON.stringify(1) with content-type application/json
   - After clicking generate-round-btn, the algorithm runs client-side. The pod algorithm `generatePods()` runs in the browser since it's a pure function imported by AdminControls. It WILL execute and produce valid assignments, then the hook sends the POST.
   - After intercepting the RPC, override the rounds GET and pods GET intercepts to return round-1 data + pods data (use fixture files)
   - Also override currentRound to return the round-1 object
   - Click generate-round-btn, wait for the RPC call
   - Assert: round-display should be visible, round-number should contain "Round 1"
   - Assert: pod cards are rendered (at least pod-card-1 or pod-card-bye)
3. "shows error when fewer than 4 active players" —
   - Setup with only 3 active players
   - Click generate-round-btn
   - The client-side algorithm throws an error for <4 players
   - Assert: Sonner toast error should appear. Test by checking for toast element with error message about "fewer than 4" or "at least 4"
   - Note: Sonner renders toasts in a `[data-sonner-toaster]` container with `[data-sonner-toast]` children. Use `cy.get('[data-sonner-toast]')` to assert error toast.
4. "shows round count in admin controls after generation" —
   - Setup with 1 existing round in the rounds mock
   - Assert admin-controls contains "Round 1"
5. "generates multiple rounds sequentially" —
   - Setup with 9 players, mock round 1 data
   - Override after first generate: update rounds to include round-2, update currentRound to round-2
   - Click generate-round-btn, intercept RPC for round 2
   - Assert "Round 2" appears in round-display

**cypress/e2e/pod-display.cy.js** — Tests for pod card rendering. Set up page with an existing round and pods (use fixture data).

Helper: `setupPageWithPods(options)` — sets up event page with round data and pod data already loaded (admin OR non-admin, with existing round + pods).

Test cases:
1. "displays non-bye pod cards with pod numbers" — Assert pod-card-1 and pod-card-2 are visible, each containing "Pod 1" / "Pod 2"
2. "displays seat ordinals in pod cards" — Assert pod-seat-1, pod-seat-2, pod-seat-3, pod-seat-4 exist within pod-card-1. Verify text content: "1st", "2nd", "3rd", "4th"
3. "displays player names in pods" — Assert pod-player-player-1 through pod-player-player-4 exist within pod-card-1 with correct names
4. "displays bye pod with distinct styling and no seat numbers" — Assert pod-card-bye exists, contains player name "Ivan", does NOT contain any pod-seat-* elements
5. "displays round heading with correct number" — Assert round-number contains "Round 1"
6. "highlights current player in pod" — Set localStorage player identity to one of the pod players. Assert that player's pod-player element has "(You)" text.
7. "displays pod border colors" — Assert pod-card-1 has a left border color (blue #3b82f6). This verifies the POD_COLORS cycling. Use `should('have.css', 'border-left-color')` or check the inline style.

Important intercept note for tests with existing rounds: The intercepts need to handle both useRounds (returns array) and useCurrentRound (returns single object). Both query `**/rest/v1/rounds*`. The simplest approach:
- Return the rounds array for ALL `**/rest/v1/rounds*` requests. supabase-js handles `.maybeSingle()` client-side based on the `Accept` header and response format.
- OR: intercept specifically based on query parameters. useCurrentRound adds `limit=1` to the URL.

Simplest working approach: Intercept `**/rest/v1/rounds*` and check if the request URL contains `limit=1` to differentiate. If it does, return a single-object response for currentRound. Otherwise, return array for useRounds.

Actually, the most reliable approach for mocking: Use `cy.intercept` with a route handler function that inspects the request URL:
```js
cy.intercept('GET', '**/rest/v1/rounds*', (req) => {
  if (req.url.includes('limit=1')) {
    req.reply({
      statusCode: 200,
      body: round1Object,
      headers: { 'content-type': 'application/vnd.pgrst.object+json; charset=utf-8' }
    })
  } else {
    req.reply({ statusCode: 200, body: roundsArray })
  }
}).as('getRounds')
```

For pods, there are two hooks: usePods (by roundId) and useAllRoundsPods (by multiple roundIds using .in()). Both query `**/rest/v1/pods*`. The usePods hook has `round_id=eq.{id}` and useAllRoundsPods has `round_id=in.({ids})`. Differentiate by checking for `in.` vs `eq.` in the URL, or just return the same pods array for all pod queries.
  </action>
  <verify>
    <automated>npx cypress run --spec "cypress/e2e/generate-round.cy.js,cypress/e2e/pod-display.cy.js" --headless 2>&1 | tail -20</automated>
  </verify>
  <done>
    - generate-round.cy.js has 5 tests covering admin controls visibility, round generation success, <4 player error, round count display, multi-round generation
    - pod-display.cy.js has 7 tests covering pod numbers, seat ordinals, player names, bye pod distinction, round heading, current player highlight, pod border colors
    - All tests pass
    - All existing Phase 1 Cypress tests continue to pass (run full suite to verify)
  </done>
</task>

</tasks>

<verification>
1. Run `npx cypress run --spec "cypress/e2e/admin-passphrase.cy.js,cypress/e2e/generate-round.cy.js,cypress/e2e/pod-display.cy.js" --headless` — all new tests pass
2. Run `npx cypress run --headless` — all tests pass (Phase 1 + Phase 2 specs)
3. Verify fixture files are valid JSON and match PostgREST response format
</verification>

<success_criteria>
- 15+ new E2E test cases across 3 spec files
- Admin passphrase modal flow tested (open, submit, cancel)
- Round generation flow tested (success, <4 error, multi-round)
- Pod display tested (seat ordinals, bye pod, player names, border colors, current player highlight)
- Zero regression on existing Phase 1 E2E tests
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-phase-2-e2e-and-integration-tests/02.1-01-SUMMARY.md`
</output>
