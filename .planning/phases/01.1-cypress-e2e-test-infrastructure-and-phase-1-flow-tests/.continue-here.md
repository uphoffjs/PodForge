---
phase: 01.1-cypress-e2e-test-infrastructure-and-phase-1-flow-tests
task: checkpoint
total_tasks: 3 plans (2 complete, 1 at checkpoint)
status: in_progress
last_updated: 2026-02-23T16:31:38.527Z
---

<current_state>
Phase 1.1 execution is mid-Wave 2. Plans 01.1-01 and 01.1-02 are fully complete. Plan 01.1-03 (visual regression) completed Task 1 (spec + baseline generation) but is paused at Task 2 (visual baseline approval checkpoint).

ADDITIONALLY: The user just made three UI changes that need baselines regenerated before approval:
1. App renamed from "Commander Pod Pairer" to "PodForge" (commit 8e4ae6c)
2. Mobile landing page layout: arrow button now on separate line under input
3. Empty player list state: now shows instructive message instead of plain text

These changes are committed but visual regression baselines are stale (generated before the rename/fixes). Baselines must be regenerated before the checkpoint can be approved.
</current_state>

<completed_work>

- Plan 01.1-01: Cypress infrastructure + data-testid attributes - DONE (commits 3a7ede5, d371f2b, 8bedb38)
  - Cypress installed with ESM config, visual regression plugin, custom commands
  - 4 custom commands: getByTestId, mockEventPage, createRealEvent, mockLandingPage
  - 3 fixture files (event, players, error-duplicate)
  - GitHub Actions CI workflow (.github/workflows/cypress.yml)
  - ESLint with eslint-plugin-cypress scoped to cypress/**
  - 33 data-testid attributes across 8 React components
- Plan 01.1-02: Core E2E test specs - DONE (commits 6d87cbd, 0e81675, 19d060b)
  - 24 tests passing across 5 spec files
  - event-creation.cy.js (7 tests), player-join.cy.js (6), duplicate-name.cy.js (3), self-drop.cy.js (5), qr-code.cy.js (3)
  - All mocked via cy.intercept()
- Plan 01.1-03 Task 1: Visual regression spec created + baselines generated (commit 0df0eb2)
  - 12 tests (4 page states x 3 breakpoints)
  - Baselines in cypress/snapshots/base/ BUT NOW STALE due to rename
- App rename + UI fixes: "Commander Pod Pairer" → "PodForge" (commit 8e4ae6c)
  - Renamed in LandingPage, index.html, package.json, storage key prefixes, all test files
  - Mobile layout fix: arrow on separate line
  - Empty state: instructive message with heading + description
</completed_work>

<remaining_work>

- Regenerate visual regression baselines (old baselines show "Commander Pod Pairer", need "PodForge"):
  ```bash
  npm run dev -- --host &
  # Wait for server, then:
  CYPRESS_visualRegressionType=base npx cypress run --spec cypress/e2e/visual-regression.cy.js
  ```
- Get user approval on new baselines (Plan 01.1-03 Task 2 checkpoint)
- Commit approved baselines
- Create 01.1-03-SUMMARY.md
- Run phase verification (gsd-verifier)
- Update ROADMAP.md with phase completion

POST-PHASE WORK (user requested during execution):
- Fix Phase 1 bugs from VERIFICATION-CHECKLIST.md:
  - Flow 2: Player joining doesn't work
  - Flow 3: Real-time updates don't work, random flashing
  - Flow 5: Self-drop doesn't work
- Add unit + integration tests
- Set up Stryker.js mutation testing (now in CLAUDE.md as a project-wide rule)
- Run mutation testing on all new/updated tests (>=80% mutation score required)
- Re-run E2E tests against fixed code to verify
</remaining_work>

<decisions_made>

- User chose comprehensive E2E coverage (happy + error + edge cases)
- Skip real-time multi-client simulation in E2E
- Most tests mock Supabase via cy.intercept(); some hit real Supabase (Claude's discretion)
- Custom commands pattern (no Page Objects), specs organized by user flow
- JavaScript for Cypress files (not TypeScript)
- GitHub Actions CI with screenshots + videos on failure, no retries, block merges
- App renamed to "PodForge" mid-execution
- Stryker.js mutation testing added to CLAUDE.md as mandatory for all test work
</decisions_made>

<blockers>
- Visual baselines are stale after the PodForge rename — must regenerate before checkpoint approval
- Phase 1 bugs (flows 2, 3, 5) are known but out of scope for Phase 1.1 (test infrastructure only)
</blockers>

<context>
The user's overall intent is a test-first approach: write E2E tests (Phase 1.1), then fix Phase 1 bugs using those tests to verify, then add unit/integration tests, then run Stryker mutation testing on everything. Phase 1.1 plans only cover Cypress E2E — the bug fixes, unit tests, and Stryker setup will need additional plans or a new phase (possibly 1.2).

The package-lock.json still has "pod-pairer" in the lockfile name fields — running `npm install` will update it automatically.

Plan 01.1-03 executor agent ID: aebbe67883bd6cd11 (can be resumed for continuation after baseline approval).
</context>

<next_action>
1. Start dev server: `npm run dev -- --host &`
2. Regenerate baselines: `CYPRESS_visualRegressionType=base npx cypress run --spec cypress/e2e/visual-regression.cy.js`
3. Present baselines to user for approval
4. On approval: commit baselines, create 01.1-03-SUMMARY.md, run verifier, complete phase
5. Then address Phase 1 bug fixes + Stryker setup (new plans needed)
</next_action>
