# Phase 1.3: Missing E2E Coverage, Baselines, and Phase Verification - Context

**Gathered:** 2026-02-23
**Status:** Ready for planning

<domain>
## Phase Boundary

Fill E2E test coverage gaps (AddPlayerForm admin flow, skip-btn click state transition), generate visual regression baselines at 3 breakpoints x 4 page states, and produce VERIFICATION.md for Phases 1 and 1.1 — closing the milestone audit. Phase 1.2 already fixed the critical bugs (race condition, wrong RPC params, content-type, localStorage format, duplicate testids). This phase builds on those fixes.

</domain>

<decisions>
## Implementation Decisions

### Verification verdicts
- Mark requirements as "verified" when user-facing behavior works correctly, even if minor tech debt remains (e.g., orphaned `validate_passphrase` RPC, unused `setPassphrase` export)
- Tech debt items that don't affect user behavior should be noted but not block verification
- Requirements affected by Phase 1.2 fixes should be re-tested against current codebase state, not audit-time state
- VERIFICATION.md should reference specific test files/commands as evidence

### Visual baseline coverage
- 3 breakpoints: mobile (375px), tablet (768px), desktop (1280px)
- 4 page states: landing/create page, event page with join form (new visitor), event page with player list (joined user), event page admin view
- Baselines should be captured after Phase 1.2 fixes are in place (correct localStorage format, fixed content-type)
- Use existing visual regression test infrastructure from Phase 1.1

### AddPlayerForm test depth
- Happy path: admin sees form, types name, submits, player appears in list
- Error: empty name submission shows validation error
- Error: duplicate name submission shows error message
- Form visibility: only renders when user is admin (not visible to regular visitors)
- Network failure handling is Claude's discretion (test if straightforward, skip if it requires complex mocking)

### Audit gap priorities
- All 5 success criteria are required — no deferrals within this phase
- Findings already fixed by Phase 1.2 (Findings 1-4, 6, 9): verify the fixes, don't re-fix
- Finding 5 (AddPlayerForm zero E2E coverage): new tests required
- Finding 7 (orphaned validate_passphrase RPC): note in VERIFICATION.md as tech debt, don't block verification
- Finding 8 (wrong sessionStorage prefix in docs): documentation-only, note but don't block
- Unexecuted plan 01.1-03 (visual baselines): covered by this phase's baseline generation
- Unexecuted plan 01-05 (human verification checkpoint): covered by this phase's VERIFICATION.md

### Claude's Discretion
- All implementation decisions delegated — user trusts Claude to make sensible choices
- Exact visual regression tooling and screenshot approach
- VERIFICATION.md format and evidence structure
- Test assertion granularity beyond the scenarios listed above
- Whether to split into 2 plans or consolidate based on task dependencies
- Error handling test depth for network failures

</decisions>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches. User's guidance: "Just take care of all of it."

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 01.3-missing-e2e-coverage-baselines-and-verification*
*Context gathered: 2026-02-23*
