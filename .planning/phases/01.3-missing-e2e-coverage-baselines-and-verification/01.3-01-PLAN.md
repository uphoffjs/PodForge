---
phase: 01.3-missing-e2e-coverage-baselines-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cypress/e2e/admin-add-player.cy.js
  - cypress/e2e/player-join.cy.js
  - cypress/e2e/visual-regression.cy.js
autonomous: true
requirements:
  - INFR-04

must_haves:
  truths:
    - "AddPlayerForm E2E tests cover admin form display, successful submit, validation error, and duplicate name error"
    - "Skip-btn click test verifies full state transition from join form to player list view"
    - "Visual regression baselines include admin view state at all 3 breakpoints"
  artifacts:
    - path: "cypress/e2e/admin-add-player.cy.js"
      provides: "AddPlayerForm E2E test coverage"
      min_lines: 50
    - path: "cypress/e2e/player-join.cy.js"
      provides: "Skip-btn click state transition test"
      contains: "join-skip-btn"
    - path: "cypress/e2e/visual-regression.cy.js"
      provides: "Admin view visual regression baselines"
      contains: "admin"
  key_links:
    - from: "cypress/e2e/admin-add-player.cy.js"
      to: "src/components/AddPlayerForm.tsx"
      via: "data-testid selectors"
      pattern: "add-player-(form|name-input|submit-btn|error)"
    - from: "cypress/e2e/admin-add-player.cy.js"
      to: "src/hooks/useAdminAuth.ts"
      via: "sessionStorage mock"
      pattern: "podforge_admin_"
    - from: "cypress/e2e/player-join.cy.js"
      to: "src/pages/EventPage.tsx"
      via: "skip-btn click -> skippedJoin state"
      pattern: "join-skip-btn.*click"
---

<objective>
Write E2E tests for AddPlayerForm admin flow, add skip-btn click state transition test, and add admin view visual regression baseline.

Purpose: Close audit Findings 5 (AddPlayerForm zero E2E coverage) and 6 (skip-btn click untested), and add the admin view visual state that is currently missing from baseline coverage.

Output: New admin-add-player.cy.js spec, updated player-join.cy.js with skip-btn click test, updated visual-regression.cy.js with admin view baselines.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/AddPlayerForm.tsx
@src/hooks/useAddPlayer.ts
@src/hooks/useAdminAuth.ts
@src/pages/EventPage.tsx
@cypress/support/commands.js
@cypress/e2e/player-join.cy.js
@cypress/e2e/visual-regression.cy.js
@cypress/e2e/duplicate-name.cy.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AddPlayerForm E2E test spec</name>
  <files>cypress/e2e/admin-add-player.cy.js</files>
  <action>
Create `cypress/e2e/admin-add-player.cy.js` with tests for the admin add-player flow. Follow the exact patterns from existing specs (cy.getByTestId, cy.mockEventPage, cy.intercept).

**Admin mock setup:** To make the AddPlayerForm visible, the user must be an admin. The `useAdminAuth` hook reads from `sessionStorage` with key `podforge_admin_{eventId}`. In `beforeEach` (or per-test), after `cy.mockEventPage()` loads, set sessionStorage via `cy.window().then(win => win.sessionStorage.setItem('podforge_admin_test-uuid', 'testpass'))`. Also set localStorage player identity so the join form is not shown (same as visual-regression.cy.js "event-page" test pattern: `win.localStorage.setItem('podforge_player_test-uuid', 'player-1')`). Then revisit the page or trigger a re-render.

Actually, since `useAdminAuth` reads sessionStorage at component mount via `useState(() => sessionStorage.getItem(...))`, the sessionStorage must be set BEFORE the page renders. Use `cy.visit('/event/test-uuid', { onBeforeLoad(win) { ... } })` pattern to set both sessionStorage and localStorage before React mounts — same pattern used in self-drop.cy.js for localStorage.

Since `cy.mockEventPage` calls `cy.visit()` internally, we cannot use it directly. Instead, replicate the mockEventPage intercepts manually (copy the event/players/realtime intercepts from commands.js) and then call `cy.visit` with `onBeforeLoad`. This is the cleanest approach.

**Test cases (4 tests in 1 describe block):**

1. **"shows add player form for admin users"** — Set up admin sessionStorage + player identity localStorage in onBeforeLoad, mock event page, verify `[data-testid="add-player-form"]` is visible and contains the input and submit button. Also verify that without admin sessionStorage, the form is NOT visible (negative test — use a separate `it` block or assert before setting storage).

2. **"admin can add a player successfully"** — Set up admin + player identity, mock event page with one existing player (Alice). Intercept POST to `**/rest/v1/players*` returning 201 with new player. Intercept GET `**/rest/v1/players*` (override) returning updated list with new player. Type "Dave" in `add-player-name-input`, click `add-player-submit-btn`. Wait for POST intercept. Verify player list contains "Dave". Verify input is cleared after success.

3. **"shows validation error for short name"** — Set up admin, type "A" (1 char, below 2-char minimum), click submit. Verify `[data-testid="add-player-error"]` shows "Name must be at least 2 characters." Verify NO POST request was made (use `cy.get('@addPlayer').should('not.exist')` pattern — actually, since no intercept alias is set for this case, just verify the error text appears and the POST was never called by checking the error element appears immediately without network activity).

4. **"shows error for duplicate player name"** — Set up admin, intercept POST to `**/rest/v1/players*` returning 409 with `{ code: '23505', message: 'duplicate key' }`. Type "Alice", click submit. Wait for POST. Verify `[data-testid="add-player-error"]` shows "That name is already taken."

**Do NOT test network failure handling** (per CONTEXT: Claude's discretion, skip if complex mocking).

Follow existing code patterns:
- Use `cy.getByTestId()` for all selectors
- Use `cy.intercept()` for API mocking
- Use `.should()` assertions that auto-retry
- Clean up localStorage and sessionStorage in `beforeEach`
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/admin-add-player.cy.js</automated>
    <manual>Verify 4 test cases pass: form visibility, successful add, validation error, duplicate error</manual>
  </verify>
  <done>AddPlayerForm has E2E tests covering admin form display, successful player add, short name validation error, and duplicate name error — closing audit Finding 5</done>
</task>

<task type="auto">
  <name>Task 2: Add skip-btn click test and admin view visual baseline</name>
  <files>cypress/e2e/player-join.cy.js, cypress/e2e/visual-regression.cy.js</files>
  <action>
**Part A: Skip-btn click state transition test in player-join.cy.js**

Add a new test to the existing "Player Join" describe block in `cypress/e2e/player-join.cy.js`:

```
it('clicking skip button hides join form and shows player list', () => {
  cy.mockEventPage(undefined, [existing players fixture or inline])
  cy.getByTestId('join-form').should('be.visible')
  cy.getByTestId('join-skip-btn').click()
  cy.getByTestId('join-form').should('not.exist')
  cy.getByTestId('player-list').should('be.visible')
})
```

Use the same mock pattern as the existing "shows skip link for admin access" test. Provide at least one player in the mock data so the player list has content to verify. After clicking skip-btn, the join form should disappear (`should('not.exist')`) and the player list should become the primary view. This verifies the `setSkippedJoin(true)` state transition in EventPage.tsx.

**Part B: Admin view visual regression baseline in visual-regression.cy.js**

Add a new `describe('Visual Regression - Event Page (admin view)', ...)` block after the existing "join form" block. This captures the admin view showing AddPlayerForm, Leave Event button, and full player list.

Setup pattern:
- Use same `breakpoints` array and `waitForFonts`/`waitForContent` helpers
- Define event and players fixtures (reuse from "event-page with players" section)
- Use `cy.visit` with `onBeforeLoad` to set both:
  - `win.sessionStorage.setItem('podforge_admin_{eventId}', 'testpass')` (admin auth)
  - `win.localStorage.setItem('podforge_player_{eventId}', 'player-1')` (player identity — so join form is hidden)
- Set up the same intercepts as mockEventPage manually (events GET with pgrst content-type, players GET, realtime block)
- Wait for `[data-testid="add-player-form"]` to confirm admin view rendered
- Call `waitForFonts()` then `cy.compareSnapshot('event-admin-{breakpoint}', 0.05)`

After writing the test, run `npm run cy:base` to generate baselines for the new admin view snapshots. The existing 12 baselines will be regenerated too (they should match since no production code changed). This produces 15 total baselines (5 states x 3 breakpoints).

Wait — actually `cy:base` runs only `visual-regression.cy.js`, so it will regenerate all baselines in that spec. The existing 12 should be identical. The 3 new admin view baselines will be created fresh.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/player-join.cy.js && npx cypress run --spec cypress/e2e/visual-regression.cy.js</automated>
    <manual>Verify skip-btn test passes (7 total player-join tests) and visual regression tests pass including new admin view baselines (15 tests total)</manual>
  </verify>
  <done>Skip-btn click test verifies join form hides and player list appears (closing audit Finding 6). Admin view visual baselines generated at 3 breakpoints. Visual regression spec now covers 5 page states x 3 breakpoints = 15 baselines.</done>
</task>

</tasks>

<verification>
1. `npx cypress run --spec cypress/e2e/admin-add-player.cy.js` — all 4 AddPlayerForm tests pass
2. `npx cypress run --spec cypress/e2e/player-join.cy.js` — all 7 tests pass (6 existing + 1 new skip-btn click)
3. `npx cypress run --spec cypress/e2e/visual-regression.cy.js` — all 15 visual regression tests pass
4. `ls cypress/snapshots/base/cypress/e2e/visual-regression.cy.js/event-admin-*` — 3 admin view baselines exist
5. No existing tests broken — `npx cypress run` full suite passes
</verification>

<success_criteria>
- AddPlayerForm has 4 E2E tests covering form display, submit, validation error, duplicate error
- Skip-btn click test verifies full state transition (join form hides, player list visible)
- Admin view visual baselines exist at mobile (375px), tablet (768px), desktop (1280px)
- All existing 36 tests still pass
- New test count: 36 + 4 (admin) + 1 (skip-btn) + 3 (admin visual) = 44 total
</success_criteria>

<output>
After completion, create `.planning/phases/01.3-missing-e2e-coverage-baselines-and-verification/01.3-01-SUMMARY.md`
</output>
