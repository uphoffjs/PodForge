---
phase: 09-timer-ui-admin-controls
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/types/database.ts
  - src/hooks/useTimer.ts
  - src/hooks/useCountdown.ts
  - src/hooks/useCountdown.test.ts
  - src/hooks/useStartTimer.ts
  - src/hooks/useStartTimer.test.ts
autonomous: true
requirements: [TIMER-02, TEST-05]

must_haves:
  truths:
    - "useTimer fetches a pending (not-started) timer row, not just running/paused"
    - "useCountdown returns a static not-started state for status='pending' (no setInterval, no ticking)"
    - "useCountdown's static state displays '{duration_minutes}:00' with phase='not-started'"
    - "useStartTimer calls rpc('start_timer', {p_event_id, p_passphrase}) and invalidates ['timer', eventId]"
    - "useStartTimer maps an invalid-passphrase error to toast.error('Invalid passphrase'), else 'Failed to start timer'"
  artifacts:
    - path: "src/hooks/useStartTimer.ts"
      provides: "passphrase-gated start mutation (verbatim shape of useResumeTimer)"
      exports: ["useStartTimer"]
    - path: "src/hooks/useStartTimer.test.ts"
      provides: "5-case mutation test (100% Stryker target)"
    - path: "src/hooks/useCountdown.ts"
      provides: "not-started early-return branch + widened phase union"
      contains: "not-started"
  key_links:
    - from: "useCountdown"
      to: "CountdownState.phase"
      via: "early-return BEFORE derivePhase when status==='pending'"
      pattern: "status === 'pending'"
    - from: "useStartTimer"
      to: "start_timer RPC"
      via: "supabase.rpc('start_timer', { p_event_id, p_passphrase })"
      pattern: "start_timer"
    - from: "useTimer"
      to: "round_timers query"
      via: ".in('status', ['running', 'paused', 'pending'])"
      pattern: "'pending'"
---

<objective>
Surface the migration-00006 contract in the client data layer: widen the `RoundTimer.status` union and the `useTimer` query filter to include `'pending'`, add a not-started early-return branch to `useCountdown` (static display, no interval) that preserves the 100%-Stryker exhaustiveness of `derivePhase`, and add a `useStartTimer` mutation hook (verbatim shape of `useResumeTimer`) with its own 100%-Stryker test.

Purpose: This is the hook/type layer the UI components in Plan 03 build on. The not-started branch and the start mutation are the two timer-critical units that must hit 100% Stryker (CLAUDE.md + TEST-05).
Output: pending-aware `useTimer`/types, a not-started `useCountdown`, and a tested `useStartTimer`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timer-ui-admin-controls/09-RESEARCH.md
@.planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md
@src/hooks/useCountdown.ts
@src/hooks/useResumeTimer.ts
@src/hooks/useResumeTimer.test.ts
@src/hooks/useTimer.ts
@src/types/database.ts

<interfaces>
<!-- Contracts the executor must conform to. Extracted from in-repo source 2026-06-28. -->

src/types/database.ts:42 — RoundTimer.status union (current): 'running' | 'paused' | 'cancelled'
  RoundTimer also has: duration_minutes: number; overtime_seconds: number; expires_at: string; remaining_seconds: number | null.

src/hooks/useTimer.ts:13 — current filter: .in('status', ['running', 'paused'])

src/hooks/useCountdown.ts — CountdownState (current phase union: 'main' | 'overtime' | 'countup').
  derivePhase (lines 43-55) and phaseUrgency (58-62) are 100%-Stryker covered and MUST stay untouched/exhaustive.
  The render-time return shape is { remainingSeconds, display, isOvertime, isPaused, isCancelled, urgency, phase }.
  The cancelled guard at lines 109-111 returns null. The tick effect only starts setInterval when status==='running' (line 94).

src/hooks/useResumeTimer.ts (verbatim template for useStartTimer):
  useMutation; mutationFn calls supabase.rpc('resume_timer', { p_event_id: eventId, p_passphrase: passphrase }); throws on error;
  onSuccess invalidates queryKey ['timer', eventId]; onError maps message.includes('invalid passphrase') → toast.error('Invalid passphrase') else toast.error('Failed to resume timer').

src/hooks/useResumeTimer.test.ts (verbatim template for useStartTimer.test.ts): vi.hoisted { mockRpc, mockToastSuccess, mockToastError }; vi.mock '@/lib/supabase' and 'sonner'; createWrapper QueryClient factory; 5 cases (calls rpc with args; invalidates ['timer']; no success toast; invalid-passphrase toast; generic-failure toast).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useStartTimer hook + 100%-Stryker test</name>
  <files>src/hooks/useStartTimer.ts, src/hooks/useStartTimer.test.ts</files>
  <read_first>
    - src/hooks/useResumeTimer.ts — the verbatim mutation template to copy
    - src/hooks/useResumeTimer.test.ts — the verbatim 5-case test harness to copy
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — `useStartTimer.ts` and `useStartTimer.test.ts` sections (exact swaps)
  </read_first>
  <behavior>
    - Test 1: mutate({ passphrase }) calls supabase.rpc('start_timer', { p_event_id: eventId, p_passphrase: passphrase }).
    - Test 2: on success, queryClient.invalidateQueries is called with queryKey ['timer', eventId].
    - Test 3: on success, no success toast fires.
    - Test 4: an error whose message includes 'invalid passphrase' → toast.error('Invalid passphrase').
    - Test 5: any other error → toast.error('Failed to start timer').
  </behavior>
  <action>
    Copy `useResumeTimer.ts` to `useStartTimer.ts` verbatim and change exactly three things: the exported function name `useResumeTimer` → `useStartTimer`, the RPC name `'resume_timer'` → `'start_timer'`, and the generic error string `'Failed to resume timer'` → `'Failed to start timer'`. Keep the params interface ({ passphrase: string }), the `{ p_event_id: eventId, p_passphrase: passphrase }` args, the `['timer', eventId]` invalidation, and the `message.includes('invalid passphrase')` mapping identical. Copy `useResumeTimer.test.ts` to `useStartTimer.test.ts` and apply the same three swaps across all five cases plus the import. This file is a 100%-Stryker target — every branch (success path, invalidation, invalid-passphrase, generic-error) must be asserted.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useStartTimer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useStartTimer.ts` exports `useStartTimer(eventId)` and calls `supabase.rpc('start_timer', { p_event_id: eventId, p_passphrase: passphrase })`.
    - onSuccess invalidates `['timer', eventId]`; onError branches to `'Invalid passphrase'` vs `'Failed to start timer'`.
    - `npx vitest run src/hooks/useStartTimer.test.ts` passes all 5 cases.
    - No reference to `resume_timer` or `'Failed to resume timer'` remains in either new file.
  </acceptance_criteria>
  <done>useStartTimer hook + its 5-case test exist and pass; ready for Stryker in Plan 04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen status union + useTimer filter; add useCountdown not-started branch</name>
  <files>src/types/database.ts, src/hooks/useTimer.ts, src/hooks/useCountdown.ts, src/hooks/useCountdown.test.ts</files>
  <read_first>
    - src/hooks/useCountdown.ts — the cancelled guard (109-111), derivePhase (43-55), phaseUrgency (58-62), the tick effect (80-107), the final return shape
    - src/hooks/useCountdown.test.ts — existing test structure to extend
    - src/types/database.ts:42 — the RoundTimer.status union
    - src/hooks/useTimer.ts:13 — the status filter
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — useCountdown / useTimer / database.ts sections; Pitfalls 2 and 5
  </read_first>
  <behavior>
    - Test: useCountdown(timer with status='pending', duration_minutes=80) returns a non-null state with phase='not-started', display='80:00', isPaused=false, isOvertime=false, isCancelled=false, urgency='normal'.
    - Test: that state does NOT start a setInterval (advancing fake timers by several seconds does not change `display`).
    - Test (regression): a running 80+20 timer still derives main/overtime/countup exactly as before (derivePhase/phaseUrgency unchanged).
  </behavior>
  <action>
    (1) In `src/types/database.ts`, widen `RoundTimer.status` to `'running' | 'paused' | 'cancelled' | 'pending'`. (2) In `src/hooks/useTimer.ts`, widen the query filter to `.in('status', ['running', 'paused', 'pending'])` so the not-started row is fetched (ORDER BY created_at DESC LIMIT 1 already masks stale rows). (3) In `src/hooks/useCountdown.ts`, extend `CountdownState['phase']` to `'main' | 'overtime' | 'countup' | 'not-started'`, then add a render-time short-circuit BEFORE the `derivePhase` call: when `timer.status === 'pending'`, return a static state `{ remainingSeconds: timer.duration_minutes * 60, display: `${timer.duration_minutes}:00`, isOvertime: false, isPaused: false, isCancelled: false, urgency: 'normal', phase: 'not-started' }`. Place it AFTER the cancelled `return null` guard and BEFORE `derivePhase`/`phaseUrgency` so those stay exhaustive and 100%-covered (Pitfall 5). Do NOT route not-started through derivePhase or start a setInterval for it (the existing `status === 'running'` tick guard already prevents pending from ticking — Pitfall 2). (4) Extend `useCountdown.test.ts` with the not-started behavior cases above; keep all existing cases green. This file must stay at 100% Stryker.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useCountdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `RoundTimer.status` union includes `'pending'`; `useTimer` filter is `['running', 'paused', 'pending']`.
    - `CountdownState['phase']` union includes `'not-started'`.
    - For `status==='pending'`, useCountdown returns `phase:'not-started'`, `display` equal to `'{duration_minutes}:00'`, `isPaused:false`, and starts no interval (fake-timer advance leaves display unchanged).
    - `derivePhase` and `phaseUrgency` source is unchanged (the not-started case returns before reaching them).
    - `npx vitest run src/hooks/useCountdown.test.ts` passes, including the not-started and running-regression cases.
  </acceptance_criteria>
  <done>Pending-aware types/query and a static not-started useCountdown branch land with the engine's 100%-Stryker exhaustiveness preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PostgREST RPC | `useStartTimer` forwards the admin passphrase to the SECURITY DEFINER `start_timer` RPC (server is the trust anchor) |
| server row → client render | `useCountdown` derives display state from a fetched timer row; no privileged decision is made client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | `useStartTimer` (client) | mitigate | Client gating is convenience only; authority is the server-side passphrase check in `start_timer` (Plan 01). The hook forwards the passphrase unchanged and surfaces server RAISE messages via toast |
| T-09-06 | Information Disclosure | error-message → toast mapping | accept | Only a coarse 'Invalid passphrase' / 'Failed to start timer' is shown; no raw stack or hash is surfaced |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (all libs already in package.json) |
</threat_model>

<verification>
- `npx vitest run src/hooks/useStartTimer.test.ts src/hooks/useCountdown.test.ts` green.
- After wave merge: `npx vitest run` full suite green (no regression from the widened union).
</verification>

<success_criteria>
- The data layer recognizes `'pending'` end-to-end (type, query, countdown).
- useCountdown shows a static not-started card state without ticking.
- useStartTimer is a tested, passphrase-gated mutation ready for the UI and Stryker gate.
</success_criteria>

<output>
Create `.planning/phases/09-timer-ui-admin-controls/09-02-SUMMARY.md` when done.
</output>
