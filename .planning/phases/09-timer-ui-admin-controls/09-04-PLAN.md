---
phase: 09-timer-ui-admin-controls
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - cypress/e2e/timer-80-20.cy.js
  - cypress/e2e/timer.cy.js
  - src/hooks/useCountdown.test.ts
  - src/hooks/useStartTimer.test.ts
  - src/components/TimerDisplay.test.tsx
  - src/components/TimerControls.test.tsx
  - src/components/AdminControls.test.tsx
autonomous: true
requirements: [TEST-05, TIMER-07]

must_haves:
  truths:
    - "An E2E spec selects 80+20, generates a round asserting the generate_round RPC body carries p_overtime_minutes=20, renders the READY TO START pending card, and clicks Start asserting the start_timer RPC fires"
    - "The E2E spec asserts the per-phase data-phase + label: not-started, overtime (OVERTIME), countup (OVERRUN) via computed expires_at"
    - "The existing timer.cy.js main-phase label assertion is updated to the UI-SPEC literal 'ROUND TIMER'"
    - "timer.cy.js exercises pause/resume/+5/cancel against overtime and countup mounts (TIMER-07 across phases)"
    - "useCountdown.ts and useStartTimer.ts pass Stryker at 100%"
    - "The changed components (TimerDisplay, TimerControls, AdminControls) pass Stryker at the project >=80% bar (new phase-band / pending-Start-Cancel / 80+20-preset branches killed)"
  artifacts:
    - path: "cypress/e2e/timer-80-20.cy.js"
      provides: "select/generate/start + three-phase transition E2E"
      contains: "timer-duration-80-20"
    - path: "cypress/e2e/timer.cy.js"
      provides: "updated ROUND TIMER label assertion + overtime/countup control coverage"
      contains: "ROUND TIMER"
  key_links:
    - from: "timer-80-20.cy.js"
      to: "generate_round / start_timer RPCs"
      via: "cy.intercept POST rpc/generate_round (assert p_overtime_minutes=20) + rpc/start_timer"
      pattern: "rpc/start_timer"
    - from: "timer-80-20.cy.js"
      to: "TimerDisplay data-phase"
      via: "computed expires_at per phase → assert data-phase + timer-status label"
      pattern: "data-phase"
---

<objective>
Close TEST-05 and the E2E half of TIMER-07: add `cypress/e2e/timer-80-20.cy.js` covering select-80+20 → generate (asserting the `generate_round` RPC body carries `p_overtime_minutes=20`) → the `READY TO START` pending card → explicit Start (asserting `start_timer` fires) → the main→overtime→count-up band/label transitions via computed `expires_at`; extend `cypress/e2e/timer.cy.js` to update the main-phase label assertion to the UI-SPEC literal `ROUND TIMER` and to exercise pause/resume/+5/cancel against overtime and count-up mounts; then run the Stryker gate to confirm `useCountdown.ts` and `useStartTimer.ts` are at 100%.

Purpose: Final verification layer — proves the whole 80+20 flow end-to-end and locks the timer-critical mutation coverage CLAUDE.md + TEST-05 demand.
Output: a new 80+20 E2E spec, an updated timer.cy.js, and a green 100% Stryker run on the two timer-critical files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timer-ui-admin-controls/09-RESEARCH.md
@.planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md
@.planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md
@cypress/e2e/timer.cy.js

<interfaces>
<!-- E2E harness + assertion contracts. From in-repo source + 09-RESEARCH/PATTERNS 2026-06-28. -->

cypress/e2e/timer.cy.js — `setupTimerPage({ timer, asAdmin })` (lines 26-89): blocks the Realtime socket, mocks events/players/rounds/pods, serves round_timers via the PostgREST single-object header (vnd.pgrst.object+json). Custom command `cy.getByTestId(id)`. No `cy.wait(ms)` — use intercept aliases.
  Current main-phase label assertion (line ~104): cy.getByTestId('timer-status').should('contain', 'Round Timer')  ← UPDATE to 'ROUND TIMER'.

Computed-expires_at phase mounting (RESEARCH:256-263 / PATTERNS:156-164):
  pending  : { ...base, status: 'pending', overtime_seconds: 1200 }                                            → data-phase 'not-started', label 'READY TO START', countdown '80:00'
  overtime : { ...base, status: 'running', overtime_seconds: 1200, expires_at: now - 60s }                     → data-phase 'overtime', label 'OVERTIME'
  countup  : { ...base, status: 'running', overtime_seconds: 1200, expires_at: now - (1200+60)s }              → data-phase 'countup', label 'OVERRUN'

testids in play (from Plan 03): timer-duration-80-20, generate-round-btn, timer-display (carries data-phase), timer-status, timer-countdown, timer-start-btn, timer-pause-btn, timer-resume-btn, timer-extend-btn, timer-cancel-btn.

RPC intercepts: cy.intercept('POST', '**/rest/v1/rpc/generate_round*') — assert request body p_overtime_minutes === 20; cy.intercept('POST', '**/rest/v1/rpc/start_timer*') — assert called on timer-start-btn click.

Stryker: stryker.config.mjs in repo; mutate-scoped run `npx stryker run --mutate "src/hooks/useCountdown.ts,src/hooks/useStartTimer.ts"`; 100% required on these timer-critical files (CLAUDE.md + TEST-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New timer-80-20.cy.js — select / generate / start / three-phase transitions</name>
  <files>cypress/e2e/timer-80-20.cy.js</files>
  <read_first>
    - cypress/e2e/timer.cy.js — the `setupTimerPage` intercept harness (26-89), the computed-expires_at pattern (95-99), getByTestId usage; CLAUDE.md Cypress standard (data-testid only, intercept not cy.wait)
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — `timer-80-20.cy.js` section (reuse points + per-phase mounts)
    - .planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md — labels/testids (timer-duration-80-20, timer-start-btn, READY TO START / OVERTIME / OVERRUN, data-phase)
  </read_first>
  <action>
    Create `cypress/e2e/timer-80-20.cy.js` reusing the `setupTimerPage` harness verbatim. Cover: (1) Admin selects the `timer-duration-80-20` chip and clicks `generate-round-btn`; with `cy.intercept('POST', '**/rest/v1/rpc/generate_round*')` aliased, assert the request body's `p_overtime_minutes === 20` (wait on the alias, no `cy.wait(ms)`). (2) Mount a `pending` timer (status 'pending', overtime_seconds 1200) and assert `timer-display` has `data-phase='not-started'`, `timer-status` contains `READY TO START`, `timer-countdown` shows `80:00`, and the `timer-start-btn` is visible. (3) With `cy.intercept('POST', '**/rest/v1/rpc/start_timer*')` aliased, click `timer-start-btn` and assert the intercept fired. (4) Mount the `overtime` timer (expires_at now-60s) → assert `data-phase='overtime'` + `timer-status` contains `OVERTIME`. (5) Mount the `countup` timer (expires_at now-(1200+60)s) → assert `data-phase='countup'` + `timer-status` contains `OVERRUN` and `timer-countdown` text starts with `+`. Use only `data-testid` selectors and intercept aliases — no arbitrary `cy.wait(ms)`, no DOM-conditional branching.
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/timer-80-20.cy.js</automated>
  </verify>
  <acceptance_criteria>
    - The spec selects `timer-duration-80-20`, generates, and asserts the intercepted `generate_round` body has `p_overtime_minutes === 20`.
    - It asserts the pending card: `data-phase='not-started'`, status `READY TO START`, countdown `80:00`, and a visible `timer-start-btn`.
    - Clicking `timer-start-btn` triggers an asserted `start_timer` RPC intercept.
    - It asserts `data-phase`/label for overtime (`OVERTIME`) and countup (`OVERRUN`, `+` prefix) via computed expires_at.
    - Uses `data-testid` + intercept aliases only; contains no `cy.wait(` numeric calls.
    - `npx cypress run --spec cypress/e2e/timer-80-20.cy.js` passes.
  </acceptance_criteria>
  <done>The 80+20 select→start→three-phase flow is covered end-to-end with intercept-based assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend timer.cy.js — ROUND TIMER label + pause/resume/+5/cancel across overtime & countup</name>
  <files>cypress/e2e/timer.cy.js</files>
  <read_first>
    - cypress/e2e/timer.cy.js — the running-state label assertion (~line 104, currently `should('contain', 'Round Timer')`), the paused/controls describe blocks, the computed-expires_at pattern
    - .planning/phases/09-timer-ui-admin-controls/09-RESEARCH.md — Pitfall 3 (label casing) + Decision: honor the UI-SPEC `ROUND TIMER` literal
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — Cypress intercept harness shared pattern
  </read_first>
  <action>
    (1) Update the main-phase label assertion (the `cy.getByTestId('timer-status').should('contain', 'Round Timer')` at ~line 104) to `should('contain', 'ROUND TIMER')` — honoring the locked decision to use the UI-SPEC label literal (Plan 03 renders `ROUND TIMER`). Update any other timer.cy.js assertion that expects the old `Round Timer` text content. (2) Add TIMER-07 control coverage across phases: using the computed-expires_at pattern, mount an `overtime` timer (status running, overtime_seconds 1200, expires_at now-60s) and a `countup` timer (expires_at now-(1200+60)s), and for each assert that `timer-pause-btn`, `timer-extend-btn`, and `timer-cancel-btn` are visible and operable (e.g. clicking pause fires the intercepted `pause_timer` RPC; clicking +5 fires `extend_timer`; Cancel opens the ConfirmDialog). Keep all selectors `data-testid` and use intercept aliases (no `cy.wait(ms)`).
  </action>
  <verify>
    <automated>npx cypress run --spec cypress/e2e/timer.cy.js</automated>
  </verify>
  <acceptance_criteria>
    - The main-phase label assertion asserts `ROUND TIMER` (no remaining `should('contain', 'Round Timer')` for the main label).
    - New cases mount overtime and countup timers and assert pause/+5/cancel controls render and their RPCs fire (or ConfirmDialog opens for cancel).
    - No `cy.wait(` numeric calls added; `data-testid` selectors only.
    - `npx cypress run --spec cypress/e2e/timer.cy.js` passes.
  </acceptance_criteria>
  <done>timer.cy.js matches the new label and proves controls work in overtime and count-up.</done>
</task>

<task type="auto">
  <name>Task 3: Stryker gate — 100% on timer-critical hooks + >=80% on changed components</name>
  <files>src/hooks/useCountdown.test.ts, src/hooks/useStartTimer.test.ts, src/components/TimerDisplay.test.tsx, src/components/TimerControls.test.tsx, src/components/AdminControls.test.tsx</files>
  <read_first>
    - stryker.config.mjs — runner + thresholds (break:80, high:90)
    - src/hooks/useCountdown.test.ts, src/hooks/useStartTimer.test.ts (Plan 02) — the hook tests being scored
    - src/components/TimerDisplay.test.tsx, src/components/TimerControls.test.tsx, src/components/AdminControls.test.tsx (Plan 03) — the component tests being scored
    - CLAUDE.md — Mutation Testing section (100% on critical timer logic; >=80% project minimum; no `--mutate` exclusion gaming)
  </read_first>
  <action>
    Two scoped Stryker runs. (1) TIMER-CRITICAL HOOKS at 100%: run `npx stryker run --mutate "src/hooks/useCountdown.ts,src/hooks/useStartTimer.ts"` and confirm BOTH report 100% (0 survived, 0 no-coverage). Likely survivors: the not-started early-return branch in `useCountdown` (Pitfall 5) or a start-error branch in `useStartTimer` — strengthen `useCountdown.test.ts` / `useStartTimer.test.ts` (Plan 02 files) until killed. (2) CHANGED COMPONENTS at the project >=80% bar: run `npx stryker run --mutate "src/components/TimerDisplay.tsx,src/components/TimerControls.tsx,src/components/AdminControls.tsx"` and confirm it clears the break threshold (Stryker exits non-zero below 80). Kill mutants on the NEW branches — TimerDisplay phase-band selection + not-started card, TimerControls pending Start/Cancel branch, AdminControls 80+20 preset + threaded `overtimeMinutes` — by strengthening the respective component test files. Do NOT narrow `--mutate` to game either score. Record both final scores and any inline-disabled documented equivalent mutants (with rationale, mirroring the single justified disable in Phase 8 08-03) in the SUMMARY. Presentational mutants with no behavioral impact may be documented as equivalent rather than killed, but the new-branch logic mutants must die.
  </action>
  <verify>
    <automated>npx stryker run --mutate "src/hooks/useCountdown.ts,src/hooks/useStartTimer.ts" 2>&1 | grep -Eiq "100.00%|100%" && echo HOOKS_100 && npx stryker run --mutate "src/components/TimerDisplay.tsx,src/components/TimerControls.tsx,src/components/AdminControls.tsx" >/dev/null 2>&1 && echo COMPONENTS_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx stryker run --mutate "src/hooks/useCountdown.ts,src/hooks/useStartTimer.ts"` reports 100% for both files (or any remaining mutant is an inline-disabled documented equivalent).
    - `npx stryker run --mutate "src/components/TimerDisplay.tsx,src/components/TimerControls.tsx,src/components/AdminControls.tsx"` clears the project break threshold (>=80%), with the new phase-band / pending-Start-Cancel / 80+20-preset branch mutants killed.
    - The `--mutate` scope was not narrowed to exclude lines for score inflation.
    - Both final scores are recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Timer-critical hooks are mutation-proven at 100% and the changed components clear the >=80% project bar, satisfying TEST-05 + the CLAUDE.md mutation rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Cypress intercepts mock all server responses; no live credentials or RPC reach a real DB in these specs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Repudiation / false-positive coverage | E2E suppressing real failures | mitigate | Assert RPC bodies (p_overtime_minutes=20) and concrete labels/data-phase, not just element presence; no `cy.wait(ms)`, no DOM-conditional branching that can silently pass |
| T-09-10 | Tampering | weak tests masking regressions | mitigate | Stryker 100% gate on useCountdown/useStartTimer forces assertions that actually kill mutants; no `--mutate` exclusion gaming (CLAUDE.md) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (Cypress/Stryker already in package.json) |
</threat_model>

<verification>
- `npx cypress run --spec cypress/e2e/timer-80-20.cy.js cypress/e2e/timer.cy.js` green (start the preview/dev server first per the existing Cypress convention).
- `npx vitest run` full suite green.
- `npx stryker run --mutate "src/hooks/useCountdown.ts,src/hooks/useStartTimer.ts"` at 100%.
</verification>

<success_criteria>
- E2E covers selecting + starting 80+20 and the main→overtime→count-up transitions (TEST-05 E2E).
- Controls proven operable in overtime and count-up (TIMER-07 E2E half).
- useCountdown.ts and useStartTimer.ts at 100% Stryker (TEST-05 mutation target).
</success_criteria>

<output>
Create `.planning/phases/09-timer-ui-admin-controls/09-04-SUMMARY.md` when done.
</output>
