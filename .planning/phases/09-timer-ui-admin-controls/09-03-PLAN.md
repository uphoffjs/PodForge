---
phase: 09-timer-ui-admin-controls
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/TimerDisplay.tsx
  - src/components/TimerDisplay.test.tsx
  - src/components/TimerControls.tsx
  - src/components/TimerControls.test.tsx
  - src/components/AdminControls.tsx
  - src/components/AdminControls.test.tsx
autonomous: true
requirements: [TIMER-01, TIMER-04, TIMER-07]

must_haves:
  truths:
    - "TimerDisplay selects its band by phase first; only main falls back to the existing urgency progression (no regression to plain 60/90/120 timers)"
    - "TimerDisplay renders OVERTIME (amber) and OVERRUN (red pulse) bands and a READY TO START not-started card"
    - "TimerDisplay exposes data-phase on the timer-display element for E2E phase assertions"
    - "TimerControls renders a Start Timer button (and Cancel) for a pending timer, and pause/resume/+5/cancel for running/paused"
    - "AdminControls shows an 80+20 preset chip and threads overtimeMinutes=20 into useGenerateRound"
  artifacts:
    - path: "src/components/TimerDisplay.tsx"
      provides: "phase-first band map + not-started card + data-phase attr"
      contains: "data-phase"
    - path: "src/components/TimerControls.tsx"
      provides: "pending Start+Cancel branch via useStartTimer"
      contains: "useStartTimer"
    - path: "src/components/AdminControls.tsx"
      provides: "80+20 preset object model + threaded overtimeMinutes"
      contains: "timer-duration-80-20"
  key_links:
    - from: "TimerDisplay"
      to: "useCountdown().phase"
      via: "phase-first band/label selection; main → urgencyStyles fallback"
      pattern: "countdown.phase === 'main'"
    - from: "TimerControls"
      to: "start_timer RPC"
      via: "useStartTimer(eventId).mutate({ passphrase })"
      pattern: "useStartTimer"
    - from: "AdminControls"
      to: "useGenerateRound"
      via: "generateRound.mutate({ ..., timerDurationMinutes, overtimeMinutes })"
      pattern: "overtimeMinutes"
---

<objective>
Surface the 80+20 timer in the three shipped components: make `TimerDisplay` select its band/label by `countdown.phase` first (keeping the existing urgency progression for `main` so plain 60/90/120 timers do not regress) and render the not-started `READY TO START` card; add a `pending` branch to `TimerControls` that renders the accent **Start Timer** button plus **Cancel** (the locked Cancel-on-not-started decision); and replace the `AdminControls` duration picker with a preset-object model that adds the `80+20` chip and finally threads `overtimeMinutes` into `useGenerateRound`.

Purpose: Delivers TIMER-01 (preset), TIMER-04 (phase-distinct visuals), and the UI half of TIMER-07 (controls + Start across phases), all pinned to the verified 09-UI-SPEC tokens/labels/testids.
Output: phase-aware `TimerDisplay`, pending-aware `TimerControls`, 80+20-aware `AdminControls`, each with component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md
@.planning/phases/09-timer-ui-admin-controls/09-RESEARCH.md
@.planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md
@src/components/TimerDisplay.tsx
@src/components/TimerControls.tsx
@src/components/AdminControls.tsx
@src/hooks/useStartTimer.ts

<interfaces>
<!-- Contracts + exact tokens. From in-repo source + 09-UI-SPEC.md (verified 6/6) 2026-06-28. -->

useCountdown().phase (after Plan 02): 'main' | 'overtime' | 'countup' | 'not-started'. CountdownState also exposes isPaused, isOvertime, urgency, display.

TimerDisplay.tsx current: urgencyStyles map (normal/warning/danger/expired, lines 9-14) keyed off countdown.urgency at line 30; statusLabel ternary (22-26); timer-display div with data-testid (29-32); timer-countdown + timer-status + notification-prompt blocks (33-67). Keep urgencyStyles AND the notification blocks unchanged.

09-UI-SPEC band tokens (lines 110-117):
  main-normal:  bg-surface-raised text-text-primary border-border        label ROUND TIMER
  main-warning: bg-yellow-900/30 text-yellow-400 border-yellow-700       label ROUND TIMER
  main-danger:  bg-red-900/30 text-red-400 border-red-700                label ROUND TIMER
  overtime:     bg-accent/15 text-accent-bright border-accent            label OVERTIME       (counts down M:SS)
  countup:      bg-red-900/50 text-red-300 border-red-500 animate-pulse  label OVERRUN        (+M:SS)
  not-started:  bg-surface-raised text-text-secondary border-border      label READY TO START (static {duration}:00)
  any phase isPaused: active band + opacity-70, label PAUSED

TimerControls.tsx current: props { eventId, passphrase, timer, onPassphraseNeeded }; `if (timer.status === 'cancelled') return null` (line 34); handlePassphraseRejection helper (38-42); running ? Pause : Resume branch (92-114); +5 (timer-extend-btn) and Cancel (timer-cancel-btn) buttons; ConfirmDialog (Cancel Timer?). Pause/resume/+5/cancel are already phase-agnostic (Phase 8) — no change needed for main/overtime/countup.

Accent primary button style (from AdminControls Generate button): `bg-accent ... text-surface hover:bg-accent-bright ... min-h-[44px]`. Neutral control style (pause/resume): `border border-border bg-surface-raised ... min-h-[44px]`. lucide-react `Play` is already imported in TimerControls.

AdminControls.tsx current: `selectedDuration: number | null` (line 56); picker maps `[60, 90, 120]` (line 183, data-testid `timer-duration-{n}`, selected = `bg-accent text-surface border border-accent`, unselected = `bg-surface border border-border text-text-secondary hover:border-border-bright`, min-h-[36px]); handleGenerateRound mutate passes only `timerDurationMinutes: selectedDuration ?? undefined` (line 110) and resets `setSelectedDuration(null)` on success (115). useGenerateRound already accepts `overtimeMinutes` (threaded to p_overtime_minutes in Phase 8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TimerDisplay — phase-first band/label map, not-started card, data-phase attr</name>
  <files>src/components/TimerDisplay.tsx, src/components/TimerDisplay.test.tsx</files>
  <read_first>
    - src/components/TimerDisplay.tsx — urgencyStyles (9-14), the line-30 urgency lookup, statusLabel ternary (22-26), timer-display div (29-32); keep the notification blocks (46-67) untouched
    - .planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md — Per-Phase Timer Band Tokens table (110-117), Copywriting Contract (157-168), §3 Not-started card
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — TimerDisplay section (phaseBands map + statusLabel shape)
  </read_first>
  <behavior>
    - Test: phase='main', urgency='normal' → element has data-phase='main', status text 'ROUND TIMER', band class bg-surface-raised.
    - Test: phase='main', urgency='warning'/'danger' → keeps the yellow/red urgency band (no regression).
    - Test: phase='overtime' → data-phase='overtime', status 'OVERTIME', band bg-accent/15 / border-accent.
    - Test: phase='countup' → data-phase='countup', status 'OVERRUN', band includes animate-pulse.
    - Test: phase='not-started' → data-phase='not-started', status 'READY TO START', band bg-surface-raised text-text-secondary, display '80:00'.
    - Test: isPaused (any phase) → status 'PAUSED', className includes opacity-70.
  </behavior>
  <action>
    Add a `phaseBands` const mapping `'not-started'` → `bg-surface-raised text-text-secondary border-border`, `overtime` → `bg-accent/15 text-accent-bright border-accent`, `countup` → `bg-red-900/50 text-red-300 border-red-500 animate-pulse` (exact UI-SPEC tokens). Replace the single `urgencyStyles[countdown.urgency]` lookup with a phase-first selection: when `countdown.phase === 'main'` use `urgencyStyles[countdown.urgency]` (existing progression, KEEP urgencyStyles as-is — no regression for plain timers), otherwise use `phaseBands[countdown.phase]`. Compute `dimmed = countdown.isPaused ? 'opacity-70' : ''` and append it to the className. Replace the `statusLabel` ternary with a phase-aware version: paused → `PAUSED`; else `not-started` → `READY TO START`; `overtime` → `OVERTIME`; `countup` → `OVERRUN`; default (main) → `ROUND TIMER`. Add `data-phase={countdown.phase}` to the `timer-display` element. Keep `timer-countdown` (renders `countdown.display`, which is the static `80:00` for not-started), `timer-status`, and the notification-prompt blocks unchanged. Add the component test cases above (new `TimerDisplay.test.tsx` cases or extend the existing file).
  </action>
  <verify>
    <automated>npx vitest run src/components/TimerDisplay.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `urgencyStyles` is retained and still used for `phase === 'main'` (warning/danger bands unchanged).
    - Band selection is phase-first: overtime → `bg-accent/15 text-accent-bright border-accent`; countup → includes `animate-pulse`; not-started → `bg-surface-raised text-text-secondary border-border`.
    - Status labels are exactly `ROUND TIMER` / `OVERTIME` / `OVERRUN` / `READY TO START` / `PAUSED` per phase + paused.
    - `timer-display` element carries `data-phase={countdown.phase}`; paused adds `opacity-70`.
    - `npx vitest run src/components/TimerDisplay.test.tsx` passes all six behavior cases.
  </acceptance_criteria>
  <done>TimerDisplay is phase-distinct with a not-started card and an E2E data-phase hook, with the main-phase urgency progression preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TimerControls — pending branch with Start Timer + Cancel</name>
  <files>src/components/TimerControls.tsx, src/components/TimerControls.test.tsx</files>
  <read_first>
    - src/components/TimerControls.tsx — cancelled guard (34), handlePassphraseRejection (38-42), handleResume passphrase-guard pattern (52-58), running/paused branch (92-114), Cancel button + ConfirmDialog (127-147)
    - src/hooks/useStartTimer.ts (Plan 02) — the mutation to call
    - .planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md — §2 Start Timer affordance (accent, Play icon, label, testid) + §4 controls across phases
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — TimerControls section (pending branch shape + accent style)
  </read_first>
  <behavior>
    - Test: timer.status='pending' → a timer-start-btn renders (Start Timer) and timer-cancel-btn renders; pause/resume/+5 buttons do NOT render.
    - Test: clicking timer-start-btn with a passphrase calls useStartTimer.mutate({ passphrase }).
    - Test: clicking timer-start-btn with no passphrase calls onPassphraseNeeded and does NOT mutate.
    - Test: timer.status='running' → existing Pause/+5/Cancel render (no regression); 'paused' → Resume renders.
    - Test: Cancel on a pending timer opens the ConfirmDialog and confirm calls cancelTimer.mutate.
  </behavior>
  <action>
    Add `const startTimer = useStartTimer(eventId)` (import mirrors the existing `useResumeTimer` import) and a `handleStart` that reuses the established guard: `if (!passphrase) { onPassphraseNeeded(); return }` then `startTimer.mutate({ passphrase }, { onError: handlePassphraseRejection })`. After the `if (timer.status === 'cancelled') return null` guard, add a `if (timer.status === 'pending')` branch that returns a row containing the accent **Start Timer** button (`data-testid="timer-start-btn"`, lucide `Play` icon, label `Start Timer`, accent primary style `bg-accent ... text-surface hover:bg-accent-bright ... min-h-[44px]`, `disabled={startTimer.isPending}`) AND the existing **Cancel** button + ConfirmDialog (reuse `handleCancelClick`/`handleCancelConfirm`, `data-testid="timer-cancel-btn"`) so the admin can cancel a not-yet-started round (locked decision; the migration-00006 `cancel_timer` already accepts pending). Do NOT render pause/resume/+5 in the pending branch. Leave the existing running/paused branch (pause/resume/+5/cancel) untouched — it is already phase-agnostic and covers main/overtime/countup. Add the component test cases above.
  </action>
  <verify>
    <automated>npx vitest run src/components/TimerControls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A `pending` timer renders `timer-start-btn` (label `Start Timer`, accent `bg-accent`) and `timer-cancel-btn`, but not `timer-pause-btn`/`timer-resume-btn`/`timer-extend-btn`.
    - Start with a passphrase calls `useStartTimer.mutate({ passphrase })`; Start with no passphrase calls `onPassphraseNeeded` and does not mutate.
    - Cancel on a pending timer opens the ConfirmDialog and confirm calls `cancelTimer.mutate`.
    - Running/paused branches are unchanged (Pause/Resume/+5/Cancel still render as before).
    - `npx vitest run src/components/TimerControls.test.tsx` passes all cases.
  </acceptance_criteria>
  <done>TimerControls drives the explicit start (and cancel) for pending timers while keeping the phase-agnostic running controls intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminControls — 80+20 preset object model + thread overtimeMinutes</name>
  <files>src/components/AdminControls.tsx, src/components/AdminControls.test.tsx</files>
  <read_first>
    - src/components/AdminControls.tsx — selectedDuration state (56), the `[60, 90, 120].map` picker (183-200), handleGenerateRound mutate (109-125), the on-success reset (115)
    - .planning/phases/09-timer-ui-admin-controls/09-UI-SPEC.md — §1 80+20 preset (testid, chip styles, min-h-[36px])
    - .planning/phases/09-timer-ui-admin-controls/09-PATTERNS.md — AdminControls section (PRESETS object model + threaded mutate)
  </read_first>
  <behavior>
    - Test: a chip with data-testid='timer-duration-80-20' renders alongside timer-duration-60/90/120.
    - Test: selecting the 80+20 chip applies the selected accent style; selecting then re-clicking deselects.
    - Test: with 80+20 selected, generating a round calls generateRound.mutate with timerDurationMinutes=80 and overtimeMinutes=20.
    - Test: with a plain 60/90/120 selected, mutate is called with that duration and overtimeMinutes=0.
    - Test: existing timer-duration-60/90/120 testids and behavior are preserved.
  </behavior>
  <action>
    Replace `const [selectedDuration, setSelectedDuration] = useState<number | null>(null)` with a preset-object model: a `PRESETS` array of `{ id, label, duration, overtime }` = `{60,'60 min',60,0}`, `{90,'90 min',90,0}`, `{120,'120 min',120,0}`, `{'80-20','80+20',80,20}`, and `const [selectedPreset, setSelectedPreset] = useState<(typeof PRESETS)[number] | null>(null)`. Render the picker by mapping `PRESETS` (replacing the `[60, 90, 120].map`), reusing the EXACT existing chip classes (selected `bg-accent text-surface border border-accent`; unselected `bg-surface border border-border text-text-secondary hover:border-border-bright`; `min-h-[36px]`, `flex-1`), with `data-testid={`timer-duration-${p.id}`}` (yields `timer-duration-60/90/120` stable + new `timer-duration-80-20`) and label `{p.label}`. Toggle selection (re-click deselects). In `handleGenerateRound`, thread BOTH durations into the mutate call: `timerDurationMinutes: selectedPreset?.duration`, `overtimeMinutes: selectedPreset?.overtime ?? 0` (closing the gap where overtimeMinutes was never passed). Keep the existing onSuccess/onError block, resetting `setSelectedPreset(null)` on success. Do NOT add a Start button here — Start lives on the timer card in TimerControls. Add the component test cases above.
  </action>
  <verify>
    <automated>npx vitest run src/components/AdminControls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The picker renders four chips including `data-testid="timer-duration-80-20"` (label `80+20`); 60/90/120 testids and styles unchanged.
    - Selecting 80+20 then generating calls `generateRound.mutate` with `timerDurationMinutes: 80` and `overtimeMinutes: 20`.
    - Selecting a plain preset passes its duration with `overtimeMinutes: 0`; success resets the selection.
    - No Start button is added to AdminControls.
    - `npx vitest run src/components/AdminControls.test.tsx` passes all cases.
  </acceptance_criteria>
  <done>The 80+20 preset is selectable and routes overtimeMinutes into generate_round, closing the AdminControls threading gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → mutation hooks | Component click handlers forward the passphrase to SECURITY DEFINER RPCs (start/generate/cancel); UI gating is convenience only |
| server phase state → render | TimerDisplay maps server-derived `countdown.phase` to presentation; no privileged decision client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation of Privilege | Start/Cancel handlers in TimerControls; Generate in AdminControls | mitigate | Reuse the established `if (!passphrase) onPassphraseNeeded()` guard + `handlePassphraseRejection`; real authority is the server passphrase check; AdminControls is already gated by `isAdmin` |
| T-09-08 | Tampering | overtimeMinutes routed from the preset | mitigate | Value comes from a fixed `PRESETS` table (only 0 or 20); server `generate_round` re-validates bounds (00005) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (all libs already in package.json) |
</threat_model>

<verification>
- `npx vitest run src/components/TimerDisplay.test.tsx src/components/TimerControls.test.tsx src/components/AdminControls.test.tsx` green.
- After wave merge: `npx vitest run` full suite green (no regression to plain timer display or existing controls).
</verification>

<success_criteria>
- 80+20 is a selectable preset that threads overtimeMinutes (TIMER-01).
- Each phase is visually distinct with its own label/band; main keeps its urgency progression (TIMER-04, no regression).
- Start Timer (and Cancel) work on the not-started card; pause/resume/+5/cancel keep working in all running phases (TIMER-07 UI half).
</success_criteria>

<output>
Create `.planning/phases/09-timer-ui-admin-controls/09-03-SUMMARY.md` when done.
</output>
