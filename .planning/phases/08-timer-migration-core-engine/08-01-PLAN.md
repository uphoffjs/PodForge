---
phase: 08-timer-migration-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00005_timer_overtime.sql
autonomous: false
requirements: [TIMER-03, TIMER-06]
user_setup:
  - service: supabase
    why: "Apply migration 00005 to the live database (no TS check catches schema drift — RoundTimer type is hand-maintained)"
    env_vars:
      - name: SUPABASE_DB_PASSWORD
        source: ".env (already present, gitignored) — enables non-interactive `supabase db push`"

must_haves:
  truths:
    - "Generating an 80+20 round persists overtime_seconds = 1200 on round_timers"
    - "Generating a plain timer persists overtime_seconds = 0 (backward compatible)"
    - "Pausing during overtime/count-up stores a signed (negative) remaining_seconds, not 0"
    - "Resuming after a signed pause restores the exact prior phase/position"
    - "generate_round resolves to a single candidate (no PGRST203 overload ambiguity)"
  artifacts:
    - path: "supabase/migrations/00005_timer_overtime.sql"
      provides: "overtime_seconds column + redefined generate_round (with DROPs) + pause_timer clamp removal"
      contains: "ALTER TABLE round_timers"
  key_links:
    - from: "generate_round"
      to: "round_timers.overtime_seconds"
      via: "INSERT VALUES COALESCE(p_overtime_minutes,0) * 60"
      pattern: "overtime_seconds"
    - from: "pause_timer"
      to: "round_timers.remaining_seconds"
      via: "signed EXTRACT(EPOCH ...) with no GREATEST(0,...) clamp"
      pattern: "EXTRACT\\(EPOCH"
---

<objective>
Land additive migration `00005` that makes the 80+20 timer server-authoritative: add `overtime_seconds`, redefine `generate_round` (dropping the stale overloads) to persist it, and remove the `GREATEST(0,…)` clamp from `pause_timer` so paused remaining is signed. Apply it to the live database and verify the SQL behaviors that no TS test can reach.

Purpose: This is the data-layer foundation for the entire phase. The signed-pause fix (clamp removal) is the single most important change for TIMER-06; the overload DROP prevents a runtime `PGRST203` failure.
Output: `supabase/migrations/00005_timer_overtime.sql` applied to the database, plus a documented SQL verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timer-migration-core-engine/08-CONTEXT.md
@.planning/phases/08-timer-migration-core-engine/08-RESEARCH.md
@.planning/phases/08-timer-migration-core-engine/08-PATTERNS.md
@supabase/migrations/00004_timer_system.sql
@supabase/migrations/00002_rounds_pods_admin.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00005 (column + generate_round redefine + pause_timer clamp removal)</name>
  <files>supabase/migrations/00005_timer_overtime.sql</files>
  <read_first>
    - supabase/migrations/00004_timer_system.sql — copy verbatim: generate_round body (:47-138), the timer INSERT (:110-111), pause_timer body (:145-190), the clamp line (:185), the SECURITY DEFINER + `SET search_path = public, extensions` header (:55-56), passphrase block (:68-78); confirm REPLICA IDENTITY / publication already set (:35,41)
    - supabase/migrations/00002_rounds_pods_admin.sql:83 — original 3-arg generate_round (proves the overload to DROP)
    - .planning/phases/08-timer-migration-core-engine/08-PATTERNS.md — migration section (the three required edits + "do NOT restate resume/extend/cancel")
    - .planning/phases/08-timer-migration-core-engine/08-RESEARCH.md — "generate_round overload hazard" + Pattern 2 (signed pause/resume proof)
  </read_first>
  <action>
    Create `00005_timer_overtime.sql` following the `00004` banner-comment style. Three edits only:
    (1) `ALTER TABLE round_timers ADD COLUMN overtime_seconds INTEGER NOT NULL DEFAULT 0` (0 = plain timer; 1200 = 80+20). No publication/replica change — the column rides the existing `supabase_realtime` publication.
    (2) Before redefining `generate_round`, emit `DROP FUNCTION IF EXISTS generate_round(uuid, text, jsonb);` and `DROP FUNCTION IF EXISTS generate_round(uuid, text, jsonb, integer);` to remove the 00002 and 00004 overloads (prevents PGRST203). Then recreate it copying the 00004 body verbatim, adding `p_overtime_minutes INTEGER DEFAULT 0` as the final parameter. Extend the timer INSERT to include `overtime_seconds` with value `COALESCE(p_overtime_minutes, 0) * 60`. Preserve the SECURITY DEFINER header, `SET search_path = public, extensions`, the `crypt(p_passphrase, …)` passphrase validation block, and the active-player/round-number logic verbatim (V2/V4: no new write path, no skipped passphrase). Add input validation: if `p_overtime_minutes < 0 OR p_overtime_minutes > 240` then `RAISE EXCEPTION` (bounds the new param — no negative or absurd values).
    (3) Redefine `pause_timer` copying the 00004 body verbatim, changing ONLY the clamp line: replace `remaining_seconds = GREATEST(0, EXTRACT(EPOCH FROM (v_expires_at - now()))::INTEGER)` with `remaining_seconds = EXTRACT(EPOCH FROM (v_expires_at - now()))::INTEGER` (signed).
    Do NOT restate `resume_timer`, `extend_timer`, or `cancel_timer` — Pattern 2 proves they are already correct once remaining_seconds is signed.
  </action>
  <verify>
    <automated>test -f supabase/migrations/00005_timer_overtime.sql && grep -q "overtime_seconds INTEGER NOT NULL DEFAULT 0" supabase/migrations/00005_timer_overtime.sql && grep -q "DROP FUNCTION IF EXISTS generate_round(uuid, text, jsonb, integer)" supabase/migrations/00005_timer_overtime.sql && ! grep -q "GREATEST(0, EXTRACT(EPOCH FROM (v_expires_at" supabase/migrations/00005_timer_overtime.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `ALTER TABLE round_timers ADD COLUMN overtime_seconds INTEGER NOT NULL DEFAULT 0`.
    - File contains both `DROP FUNCTION IF EXISTS generate_round(uuid, text, jsonb)` and `... (uuid, text, jsonb, integer)` BEFORE the `CREATE OR REPLACE FUNCTION generate_round`.
    - `generate_round` signature ends with `p_overtime_minutes INTEGER DEFAULT 0`; its INSERT lists `overtime_seconds` with `COALESCE(p_overtime_minutes, 0) * 60`.
    - `generate_round` RAISEs on `p_overtime_minutes < 0 OR p_overtime_minutes > 240`.
    - The `crypt(` passphrase check appears in both redefined functions.
    - `pause_timer` body contains the signed `EXTRACT(EPOCH ...)` assignment and contains NO `GREATEST(0,` clamp.
    - File does NOT contain `CREATE OR REPLACE FUNCTION resume_timer`, `extend_timer`, or `cancel_timer`.
  </acceptance_criteria>
  <done>Migration file written with exactly the three locked edits; resume/extend/cancel untouched.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration 00005 to the database</name>
  <files>supabase/migrations/00005_timer_overtime.sql</files>
  <read_first>
    - supabase/migrations/00005_timer_overtime.sql (just written)
    - .env presence of SUPABASE_DB_PASSWORD (enables non-interactive push)
  </read_first>
  <action>
    Run `supabase db push` to apply `00005` to the live database. This is BLOCKING and mandatory: build and type checks pass without it because the TS `RoundTimer` type is hand-maintained in `src/types/database.ts`, not derived from the live schema — so omitting the push produces a false-positive verification state. The push MUST happen after Task 1 (file written) and before Task 3 (live-schema verification). If `supabase db push` still demands an interactive prompt that cannot be suppressed with the stored `SUPABASE_DB_PASSWORD`, stop and surface for manual intervention (this plan is already `autonomous: false`).
  </action>
  <verify>
    <automated>supabase db push --linked 2>&1 | grep -Eiq "applied|up to date|no changes" && echo PUSHED</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits successfully (migration applied or already applied).
    - The `overtime_seconds` column exists on `round_timers` in the live DB (confirmed in Task 3).
  </acceptance_criteria>
  <done>Migration 00005 is applied to the live database.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Verify migration 00005 SQL behaviors on the live DB (Wave 0 option c)</name>
  <files>supabase/migrations/00005_timer_overtime.sql</files>
  <action>
    Human verification step — resolves the Wave 0 SQL-verification decision (RESEARCH option c: documented manual verification against the freshly-pushed DB, chosen because the repo has no pgTAP/SQL test harness and `supabase.rpc` is mocked in hook tests, so these behaviors are not machine-testable). The agent presents the checks below; the user runs them against the database the migration was pushed to and confirms.
  </action>
  <what-built>
    Migration 00005 applied to the live DB: new `overtime_seconds` column, `generate_round` redefined with the stale overloads dropped and `p_overtime_minutes` persisted, and `pause_timer`'s `GREATEST(0,…)` clamp removed so paused remaining is signed.
  </what-built>
  <how-to-verify>
    Against the database the migration was pushed to (Supabase SQL editor or psql):
    1. Confirm column: `SELECT column_name, is_nullable, column_default FROM information_schema.columns WHERE table_name='round_timers' AND column_name='overtime_seconds';` → expect `NOT NULL`, default `0`.
    2. Confirm single overload: `SELECT count(*) FROM pg_proc WHERE proname='generate_round';` → expect exactly `1`.
    3. Call `generate_round(... p_overtime_minutes => 20)` for a test event with >=4 active players; then `SELECT overtime_seconds FROM round_timers WHERE event_id = <test> ORDER BY created_at DESC LIMIT 1;` → expect `1200`. Call with `p_overtime_minutes` omitted → expect `0`.
    4. Call `generate_round(... p_overtime_minutes => -1)` → expect a RAISEd exception (bounds check).
    5. Signed pause: take a running 80+20 timer past the 80-min mark into overtime (or manually set `expires_at` to `now() - interval '5 minutes'`), call `pause_timer`, then `SELECT remaining_seconds FROM round_timers WHERE id=<timer>;` → expect a NEGATIVE value (not 0). Call `resume_timer`, then confirm `expires_at` is in the past so the client re-derives overtime/count-up (no reset to 0:00).
    6. Confirm no `PGRST203 "Could not choose the best candidate function"` error on any `generate_round` call.
  </how-to-verify>
  <verify>
    <human-check>overtime_seconds=1200 (and 0 when omitted) persists; bounds check raises on negative; single generate_round overload; paused remaining_seconds is negative; resume preserves position; no PGRST203.</human-check>
  </verify>
  <resume-signal>Type "approved" once all six checks pass; otherwise describe the failing check.</resume-signal>
  <done>SQL behaviors of migration 00005 are human-verified on the live DB (Wave 0 SQL gap closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PostgREST RPC | Untrusted admin action + passphrase crosses into SECURITY DEFINER functions |
| RPC → round_timers | Server is the sole writer of timer state (RLS allows anon SELECT only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | redefined `generate_round` / `pause_timer` | mitigate | Preserve the verbatim `crypt(p_passphrase, passphrase_hash)` validation block from 00004; the new `p_overtime_minutes` param adds no bypass and writes through the existing SECURITY DEFINER path |
| T-08-02 | Tampering / Input Validation | `p_overtime_minutes` parameter | mitigate | Server-side bounds check: `RAISE EXCEPTION` if `< 0 OR > 240`; value coerced via `COALESCE(...,0) * 60`; client never supplies phase/elapsed |
| T-08-03 | Tampering | round_timers write path | accept | No new write path or RLS change added; all writes stay behind SECURITY DEFINER RPCs (verified 00004:27-29) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit: N/A) — no registry/postinstall surface |
</threat_model>

<verification>
- Migration file passes the Task 1 grep gate (column, DROPs, signed pause, no clamp, no restated RPCs).
- `supabase db push` applies cleanly.
- Human checkpoint confirms overtime_seconds persistence (1200/0), bounds rejection, single overload, and signed-pause/resume position preservation on the live DB.
</verification>

<success_criteria>
- overtime_seconds column exists (NOT NULL DEFAULT 0); existing rows backfill to 0 (plain timers unchanged).
- generate_round persists overtime_seconds = p_overtime_minutes*60 and resolves without PGRST203.
- pause_timer stores signed remaining_seconds; resume restores prior overtime/count-up position.
</success_criteria>

<output>
Create `.planning/phases/08-timer-migration-core-engine/08-01-SUMMARY.md` when done.
</output>
