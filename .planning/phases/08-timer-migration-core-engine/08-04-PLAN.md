---
phase: 08-timer-migration-core-engine
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - src/hooks/useTimerNotification.ts
  - src/hooks/useTimerNotification.test.ts
autonomous: true
requirements: [TIMER-05]

must_haves:
  truths:
    - "A notification fires exactly once at main→overtime ('Overtime started')"
    - "A notification fires exactly once at overtime→count-up ('Round over')"
    - "Repeated re-renders / Realtime row churn in the same phase fire nothing"
    - "A client mounting directly into overtime/count-up fires nothing (refresh-safe)"
    - "A new timer.id resets the dedup set so the next round notifies fresh"
    - "Notification branches pass Stryker mutation testing at 100%"
  artifacts:
    - path: "src/hooks/useTimerNotification.ts"
      provides: "phase-transition detection + per-boundary Set dedup"
      contains: "prevPhase"
    - path: "src/hooks/useTimerNotification.test.ts"
      provides: "boundary/dedup/refresh-guard/reset/paused tests"
      contains: "countup"
  key_links:
    - from: "useTimerNotification.ts"
      to: "countdown.phase"
      via: "prev !== phase transition detection"
      pattern: "phase"
---

<objective>
Convert `useTimerNotification` from the single-boundary `remaining <= 0` trigger to phase-transition detection that fires exactly once at each boundary (main→overtime, overtime→count-up), deduped per boundary via a `Set` keyed by `${timer.id}:boundary`, and guarded against firing for boundaries crossed before mount.

Purpose: Delivers TIMER-05 and depends on the `phase` field added in Plan 03. Keying on `${timer.id}:boundary` survives Realtime row replacement; the `prevPhaseRef === null` guard makes refresh-into-overtime safe.
Output: Dual-boundary `useTimerNotification.ts` + tests at 100% mutation score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timer-migration-core-engine/08-CONTEXT.md
@.planning/phases/08-timer-migration-core-engine/08-RESEARCH.md
@.planning/phases/08-timer-migration-core-engine/08-PATTERNS.md

<interfaces>
From src/hooks/useTimerNotification.ts (current — replace ONLY the dedup ref + trigger effect; keep isSupported/permission/requestPermission + iOS-PWA try/catch):
```typescript
// :29  const lastNotifiedTimerIdRef = useRef<string | null>(null)   ← replace
// :42-62 effect fires when remainingSeconds <= 0 && isOvertime      ← replace with phase-transition detection
// :65-69 reset effect on timer.id change                           ← extend to clear both refs
```
After Plan 03, CountdownState carries `phase: 'main' | 'overtime' | 'countup'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase-transition notification with per-boundary Set dedup</name>
  <files>src/hooks/useTimerNotification.ts, src/hooks/useTimerNotification.test.ts</files>
  <read_first>
    - src/hooks/useTimerNotification.ts — isSupported/permission/requestPermission surface (:14-39), the trigger effect (:42-62), the iOS-PWA try/catch around `new Notification(...)` (:50-58), the reset effect (:65-69)
    - .planning/phases/08-timer-migration-core-engine/08-RESEARCH.md — Pattern 3 (the effect shape + the three correctness points) and Pitfalls 3-5
    - .planning/phases/08-timer-migration-core-engine/08-PATTERNS.md — useTimerNotification section (refs, fireOnce, reset)
    - src/hooks/useTimerNotification.test.ts — makeTimer (:7-21), makeCountdown/makeExpiredCountdown (:23-43; add a `phase` field), Notification mock (:46-76), renderHook+rerender transition pattern (:132-165)
  </read_first>
  <behavior>
    - main→overtime rerender (permission granted) fires the Notification constructor once with body 'Overtime started' and a per-boundary tag/key `${timer.id}:overtime`.
    - overtime→countup rerender fires once with body 'Round over', key `${timer.id}:countup`.
    - Re-render staying in the same phase fires nothing.
    - Two main→overtime transitions for the SAME timer.id fire only once total (Set dedup, robust to Realtime row churn).
    - A new timer.id clears the Set so its first boundary notifies fresh.
    - First observed render directly in 'countup' (prevPhaseRef === null) fires nothing (refresh/reconnect safe).
    - countdown.isPaused or isCancelled → no fire; permission !== 'granted' → no fire.
  </behavior>
  <action>
    Replace `lastNotifiedTimerIdRef` with `notifiedRef = useRef<Set<string>>(new Set())` and `prevPhaseRef = useRef<string | null>(null)`. Replace the trigger effect with phase-transition detection: keep the `!countdown || !timer || isPaused || isCancelled` early returns; read `phase = countdown.phase`, capture `prev = prevPhaseRef.current`, set `prevPhaseRef.current = phase`; `if (prev === null) return` (do not fire for a pre-mount boundary); `if (permission !== 'granted') return`; then `if (prev === 'main' && phase === 'overtime') fireOnce(\`${timer.id}:overtime\`, 'Overtime started')` and `if (prev === 'overtime' && phase === 'countup') fireOnce(\`${timer.id}:countup\`, 'Round over')`. Implement `fireOnce(key, body)`: return early if `notifiedRef.current.has(key)`, else `.add(key)` and construct `new Notification(title, { body, icon: '/favicon.ico', tag: key })` inside the preserved iOS-PWA try/catch. Extend the reset effect so a changed `timer.id` clears `notifiedRef` (`.clear()`) and resets `prevPhaseRef` to null. Do NOT gate on `remaining <= 0`/`=== 0` (Pitfall 5). Add a `phase` field to the test's makeCountdown/makeExpiredCountdown factories and add all <behavior> cases.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useTimerNotification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The hook uses `useRef<Set<string>>` + `prevPhaseRef`; no `lastNotifiedTimerIdRef` remains.
    - Tests assert constructor called exactly once per forward boundary with bodies 'Overtime started' / 'Round over'.
    - Same-phase rerender and prevPhaseRef===null mount-into-countup assert `.not.toHaveBeenCalled()`.
    - New timer.id resets the Set (next boundary notifies); paused/non-granted assert no fire.
    - `npx vitest run src/hooks/useTimerNotification.test.ts` passes at 100% coverage.
  </acceptance_criteria>
  <done>Notifications fire once per boundary, deduped per ${timer.id}:boundary, refresh-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Stryker mutation hardening to 100% on useTimerNotification.ts</name>
  <files>src/hooks/useTimerNotification.test.ts</files>
  <read_first>
    - .planning/phases/08-timer-migration-core-engine/08-RESEARCH.md — "Stryker mutation surface" notification cases (a)-(f)
    - stryker.config.mjs (thresholds; mutate globs src/**/*.{ts,tsx})
    - src/hooks/useTimerNotification.test.ts (tests from Task 1)
  </read_first>
  <action>
    Run Stryker scoped to `src/hooks/useTimerNotification.ts` and kill every surviving mutant on the new logic: the `prev === 'main' && phase === 'overtime'` and `prev === 'overtime' && phase === 'countup'` conditionals (each boundary tested in isolation), the `prev === null` guard (mount-into-countup test), the `notifiedRef.has(key)` dedup branch (double-transition test), the `.clear()` reset (new-timer.id test), and the string literals 'Overtime started' / 'Round over' (assert exact bodies via `toHaveBeenCalledWith`). Do not exclude files to inflate the score.
  </action>
  <verify>
    <automated>npx stryker run --mutate "src/hooks/useTimerNotification.ts" 2>&1 | grep -Eq "100.00%|100%" && echo STRYKER_100</automated>
  </verify>
  <acceptance_criteria>
    - Stryker reports 100% mutation score for `src/hooks/useTimerNotification.ts` (zero survived, zero no-coverage).
    - Notification body strings are asserted exactly (kills string-literal mutants).
    - No mutate exclusions added.
  </acceptance_criteria>
  <done>useTimerNotification.ts mutation score is 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| countdown.phase → notification side-effect | Notifications are a per-tab client concern driven by derived (server-authoritative) phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | browser Notification content | accept | Notification bodies ('Overtime started' / 'Round over') contain no PII or secrets; per-tab only |
| T-08-07 | Denial of Service | repeated re-render / Realtime churn re-firing | mitigate | Per-boundary `Set` dedup + `prev !== phase` transition gating prevents notification spam |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/hooks/useTimerNotification.test.ts` green with 100% coverage.
- `npx stryker run --mutate "src/hooks/useTimerNotification.ts"` at 100%.
- Refresh-into-phase and Realtime-churn cases confirm no spurious/duplicate fires.
</verification>

<success_criteria>
- One notification per boundary, deduped per ${timer.id}:boundary across Realtime updates.
- No fire on refresh directly into overtime/count-up.
- 100% Stryker on the new branches.
</success_criteria>

<output>
Create `.planning/phases/08-timer-migration-core-engine/08-04-SUMMARY.md` when done.
</output>
