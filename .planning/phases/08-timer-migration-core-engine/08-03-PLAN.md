---
phase: 08-timer-migration-core-engine
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - src/hooks/useCountdown.ts
  - src/hooks/useCountdown.test.ts
autonomous: true
requirements: [TIMER-03, TIMER-06]

must_haves:
  truths:
    - "Main phase counts down to 0:00 at expires_at, displaying M:SS"
    - "At the main boundary the timer shows 20:00 and enters overtime (when overtime_seconds=1200)"
    - "Overtime counts 20:00→0:00, then count-up increments +M:SS indefinitely"
    - "A paused timer derives the correct phase from signed remaining_seconds"
    - "overtime_seconds=0 reproduces today's single-phase down→count-up behavior identically"
    - "New derivation branches pass Stryker mutation testing at 100%"
  artifacts:
    - path: "src/hooks/useCountdown.ts"
      provides: "phase derivation (main/overtime/countup) + phase field on CountdownState"
      contains: "phase"
    - path: "src/hooks/useCountdown.test.ts"
      provides: "boundary + arithmetic + backward-compat phase tests"
      contains: "overtime"
  key_links:
    - from: "useCountdown.ts"
      to: "timer.overtime_seconds"
      via: "overtimeRemaining = mainRemaining + overtime_seconds"
      pattern: "overtime_seconds"
---

<objective>
Extend `useCountdown` to derive all three phases purely on the client from two server quantities (`expires_at`, `overtime_seconds`) plus the local clock, per the LOCKED single-signed-value model. Add `phase` to `CountdownState` as the new source of truth; keep `remainingSeconds`/`isOvertime`/the 4-value `urgency` union unchanged for backward compat. Prove every new branch with unit tests and 100% Stryker.

Purpose: This is the core engine of TIMER-03 and the client half of TIMER-06 — phase is a pure function of server state, so refresh/reconnect and signed-pause derive correctly "for free."
Output: Three-phase `useCountdown.ts` + comprehensive tests at 100% mutation score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timer-migration-core-engine/08-CONTEXT.md
@.planning/phases/08-timer-migration-core-engine/08-RESEARCH.md
@.planning/phases/08-timer-migration-core-engine/08-PATTERNS.md

<interfaces>
From src/hooks/useCountdown.ts (current — extend computeRemaining/CountdownState; keep formatDisplay + setInterval tick unchanged):
```typescript
export interface CountdownState {
  remainingSeconds: number
  display: string
  isOvertime: boolean
  isPaused: boolean
  isCancelled: boolean
  urgency: 'normal' | 'warning' | 'danger' | 'expired'  // DO NOT widen
}
// computeRemaining(timer): paused → remaining_seconds ?? 0 ; running → floor((expires_at - now)/1000)
// formatDisplay(n): n>=0 → "M:SS" ; n<0 → "+M:SS"
```
After Plan 02, RoundTimer has `overtime_seconds: number`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive three phases in useCountdown and add the phase field</name>
  <files>src/hooks/useCountdown.ts, src/hooks/useCountdown.test.ts</files>
  <read_first>
    - src/hooks/useCountdown.ts — computeRemaining (:19-25), computeUrgency (:27-32), formatDisplay (:34-44), CountdownState (:4-17), the setInterval tick (:50-73), the returned object (:79-86)
    - .planning/phases/08-timer-migration-core-engine/08-RESEARCH.md — Pattern 1 (the scenario table) + Open Question Q1 (urgency mapping: keep 4 values)
    - .planning/phases/08-timer-migration-core-engine/08-PATTERNS.md — useCountdown section
  </read_first>
  <behavior>
    - mainRemaining = +4500, overtime_seconds=1200 → phase 'main', display "75:00".
    - mainRemaining = 0, overtime_seconds=1200 → phase 'overtime', display "20:00".
    - mainRemaining = 1 → phase 'main' (kills the > vs >= boundary mutant).
    - Running, mainRemaining = -300, overtime_seconds=1200 → phase 'overtime', display "15:00" (kills the + vs - arithmetic mutant; 15:00 ≠ the minus result).
    - overtimeRemaining = 0 → phase 'countup'; overtimeRemaining = 1 → phase 'overtime' (kills the second boundary mutant).
    - mainRemaining = -1320, overtime_seconds=1200 → phase 'countup', display "+2:00".
    - Paused with remaining_seconds = -300, overtime_seconds=1200 → phase 'overtime', display "15:00".
    - Paused with remaining_seconds = -1320, overtime_seconds=1200 → phase 'countup', display "+2:00".
    - Backward compat: overtime_seconds=0, mainRemaining=-90 → phase 'countup', display "+1:30" (identical to today).
  </behavior>
  <action>
    Compute `mainRemaining` exactly as the existing `computeRemaining` does (paused → `remaining_seconds ?? 0`; running → `floor((expires_at - now)/1000)`). Add `const overtime = timer.overtime_seconds ?? 0` and `const overtimeRemaining = mainRemaining + overtime`. Derive `phase`: `mainRemaining > 0` → 'main' (displaySeconds = mainRemaining); else `overtimeRemaining > 0` → 'overtime' (displaySeconds = overtimeRemaining); else 'countup' (displaySeconds = overtimeRemaining, negative → formatDisplay yields "+M:SS"). Add `phase: 'main' | 'overtime' | 'countup'` to `CountdownState`. Feed `displaySeconds` (not `remainingSeconds`) into `formatDisplay` for the returned `display`. Keep `remainingSeconds = mainRemaining` and `isOvertime = mainRemaining <= 0`. Do NOT widen the `urgency` union: map main → `computeUrgency(mainRemaining)`, overtime → 'danger', countup → 'expired'. Keep `formatDisplay` and the `setInterval` tick untouched (the tick must recompute phase/display each second — drive the returned values off the per-tick derivation, not a stale closure). Add all <behavior> cases to `useCountdown.test.ts` using the existing makeTimer factory (override `overtime_seconds`) and the paused-timer pattern for signed cases.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useCountdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CountdownState` includes `phase: 'main' | 'overtime' | 'countup'`; `urgency` union is still exactly the 4 original values.
    - Every <behavior> case asserts the exact `phase` and `display` listed.
    - `remainingSeconds === mainRemaining` and `isOvertime === (mainRemaining <= 0)` (existing TimerDisplay tests stay green).
    - `npx vitest run src/hooks/useCountdown.test.ts` passes and coverage on useCountdown.ts is 100% (vite.config gate).
  </acceptance_criteria>
  <done>useCountdown derives main/overtime/countup with a phase field; all behavior cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Stryker mutation hardening to 100% on useCountdown.ts</name>
  <files>src/hooks/useCountdown.test.ts</files>
  <read_first>
    - .planning/phases/08-timer-migration-core-engine/08-RESEARCH.md — "Stryker mutation surface" (boundary operators, arithmetic, backward-compat path)
    - stryker.config.mjs (mutate globs src/**/*.{ts,tsx}; thresholds break:80 high:90 — project rule = 100% on critical timer logic)
    - src/hooks/useCountdown.test.ts (tests from Task 1)
  </read_first>
  <action>
    Run Stryker scoped to `src/hooks/useCountdown.ts` and inspect surviving mutants. Add or strengthen test assertions until every mutant on the new derivation branches is killed — specifically the `mainRemaining > 0` and `overtimeRemaining > 0` boundary operators (test at exactly 0 and 1), the `mainRemaining + overtime_seconds` arithmetic (the 15:00 case distinguishes + from -), and the `overtime_seconds ?? 0` backward-compat default (the overtime_seconds=0 count-up case kills the default-removed mutant). Do NOT exclude files from the mutate glob to inflate the score.
  </action>
  <verify>
    <automated>npx stryker run --mutate "src/hooks/useCountdown.ts" 2>&1 | grep -Eq "100.00%|100%" && echo STRYKER_100</automated>
  </verify>
  <acceptance_criteria>
    - Stryker reports 100% mutation score for `src/hooks/useCountdown.ts` (zero survived, zero no-coverage mutants).
    - No file was added to Stryker `mutate` exclusions to achieve the score.
  </acceptance_criteria>
  <done>useCountdown.ts mutation score is 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server columns → client derivation | useCountdown derives phase/elapsed solely from server-authoritative expires_at + overtime_seconds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | client phase/elapsed derivation | mitigate | Phase is a pure function of server columns (`expires_at`, `overtime_seconds`) + local clock; no client-writable timer state. A tampered client only affects its own display, never persisted state (writes are RPC+passphrase only). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/hooks/useCountdown.test.ts` green with 100% coverage.
- `npx stryker run --mutate "src/hooks/useCountdown.ts"` at 100%.
- Backward-compat case (overtime_seconds=0) confirms plain timers behave identically to today.
</verification>

<success_criteria>
- useCountdown derives main 80:00→0:00, overtime 20:00→0:00, count-up +M:SS indefinitely.
- Paused/refresh cases derive correct phase from signed remaining (TIMER-06 client half).
- 100% Stryker on the new branches.
</success_criteria>

<output>
Create `.planning/phases/08-timer-migration-core-engine/08-03-SUMMARY.md` when done.
</output>
