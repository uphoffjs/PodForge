---
phase: 08-timer-migration-core-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/hooks/useGenerateRound.ts
  - src/hooks/useGenerateRound.test.ts
  - src/hooks/useCountdown.test.ts
  - src/hooks/useTimerNotification.test.ts
  - src/components/TimerDisplay.test.tsx
  - src/components/TimerControls.test.tsx
  - src/hooks/useTimer.test.ts
  - src/pages/EventPage.test.tsx
autonomous: true
requirements: [TIMER-03]

must_haves:
  truths:
    - "RoundTimer carries a required overtime_seconds: number field"
    - "Every RoundTimer test factory builds with overtime_seconds (suite stays green)"
    - "useGenerateRound forwards overtimeMinutes as p_overtime_minutes (0 when omitted)"
  artifacts:
    - path: "src/types/database.ts"
      provides: "overtime_seconds field on RoundTimer; signed remaining_seconds comment"
      contains: "overtime_seconds"
    - path: "src/hooks/useGenerateRound.ts"
      provides: "overtimeMinutes param threaded to the generate_round RPC"
      contains: "p_overtime_minutes"
  key_links:
    - from: "useGenerateRound.ts"
      to: "generate_round RPC"
      via: "supabase.rpc arg p_overtime_minutes: overtimeMinutes ?? 0"
      pattern: "p_overtime_minutes"
---

<objective>
Define the data-layer contract the rest of the phase builds on: add `overtime_seconds` to the `RoundTimer` type, keep every RoundTimer test factory compiling (type stays green across all waves), and thread an `overtimeMinutes` param through `useGenerateRound` to the `generate_round` RPC.

Purpose: This is the interface-first task — Plans 03 (useCountdown) and 04 (useTimerNotification) consume `overtime_seconds`; defining the type and fixing all consumers here keeps the full test suite green between waves.
Output: Extended `RoundTimer` type, repo-wide factory compatibility, `useGenerateRound` carrying `overtimeMinutes`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timer-migration-core-engine/08-CONTEXT.md
@.planning/phases/08-timer-migration-core-engine/08-RESEARCH.md
@.planning/phases/08-timer-migration-core-engine/08-PATTERNS.md

<interfaces>
From src/types/database.ts (current RoundTimer — add overtime_seconds):
```typescript
export type RoundTimer = {
  id: string
  round_id: string
  event_id: string
  duration_minutes: number
  status: 'running' | 'paused' | 'cancelled'
  started_at: string
  remaining_seconds: number | null
  paused_at: string | null
  expires_at: string
  created_at: string
}
```

From src/hooks/useGenerateRound.ts (current RPC call — mirror p_timer_duration_minutes):
```typescript
interface GenerateRoundParams { passphrase: string; podAssignments: PodAssignment[]; timerDurationMinutes?: number }
await supabase.rpc('generate_round', { p_event_id, p_passphrase, p_pod_assignments, p_timer_duration_minutes: timerDurationMinutes ?? null })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overtime_seconds to RoundTimer and fix all RoundTimer test factories</name>
  <files>src/types/database.ts, src/hooks/useCountdown.test.ts, src/hooks/useTimerNotification.test.ts, src/components/TimerDisplay.test.tsx, src/components/TimerControls.test.tsx, src/hooks/useTimer.test.ts, src/pages/EventPage.test.tsx</files>
  <read_first>
    - src/types/database.ts (RoundTimer at the timer block — add the field)
    - .planning/phases/08-timer-migration-core-engine/08-PATTERNS.md — database.ts section (field + comment style) and the "Ripple" note (every factory must add overtime_seconds: 0)
    - src/hooks/useCountdown.test.ts (makeTimer factory ~:6-20)
    - src/hooks/useTimerNotification.test.ts (makeTimer factory ~:7-21)
    - src/components/TimerDisplay.test.tsx, src/components/TimerControls.test.tsx, src/hooks/useTimer.test.ts, src/pages/EventPage.test.tsx (each constructs a RoundTimer literal containing `expires_at:`)
  </read_first>
  <action>
    Add `overtime_seconds: number` to the `RoundTimer` type (place it next to `remaining_seconds`; update the `remaining_seconds` comment to note it is now SIGNED — negative = into overtime/count-up — and comment `overtime_seconds` as "0 for plain timers, 1200 for 80+20"). Because the field is required (non-nullable), every RoundTimer literal/factory in the repo must add `overtime_seconds: 0` or TS will not compile. Mechanically add `overtime_seconds: 0` to the default object of every RoundTimer factory/literal in the six listed test files. Do NOT add a `phase` field to any CountdownState factory here — `phase` does not exist until Plan 03; only the RoundTimer factories change in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/hooks/useTimer.test.ts src/components/TimerControls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `RoundTimer` includes `overtime_seconds: number` (required, not optional).
    - `npx tsc --noEmit` passes with zero errors (proves every factory was updated).
    - The full existing suite still passes (no behavior change introduced by the field).
  </acceptance_criteria>
  <done>overtime_seconds is a required RoundTimer field and the whole project type-checks and tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread overtimeMinutes through useGenerateRound</name>
  <files>src/hooks/useGenerateRound.ts, src/hooks/useGenerateRound.test.ts</files>
  <read_first>
    - src/hooks/useGenerateRound.ts (GenerateRoundParams :11-15; mutationFn rpc call :21-27 — mirror p_timer_duration_minutes at :26)
    - src/hooks/useGenerateRound.test.ts (vi.hoisted mockRpc :8-20; createWrapper :22-28; the toHaveBeenCalledWith assertion ~:63-68; the "passes null when not provided" test ~:71-91)
    - .planning/phases/08-timer-migration-core-engine/08-PATTERNS.md — useGenerateRound section
  </read_first>
  <behavior>
    - When `overtimeMinutes: 20` is passed, `supabase.rpc('generate_round', …)` is called with `p_overtime_minutes: 20`.
    - When `overtimeMinutes` is omitted, the rpc is called with `p_overtime_minutes: 0`.
    - The existing args (p_event_id, p_passphrase, p_pod_assignments, p_timer_duration_minutes) are still forwarded unchanged.
  </behavior>
  <action>
    Add `overtimeMinutes?: number` to `GenerateRoundParams`. Destructure it in `mutationFn` and add `p_overtime_minutes: overtimeMinutes ?? 0` to the `supabase.rpc('generate_round', {...})` call, mirroring the existing `p_timer_duration_minutes` threading. Leave `useMutation`, the five `invalidateQueries` keys, and the `onError` toast block unchanged. Add the two assertion tests described in <behavior> using the existing `toHaveBeenCalledWith` idiom (these kill the arg-drop / default-removed mutant on the new param).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useGenerateRound.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useGenerateRound.ts` rpc call contains `p_overtime_minutes: overtimeMinutes ?? 0`.
    - Test asserts `p_overtime_minutes: 20` is forwarded when passed and `p_overtime_minutes: 0` when omitted, via `toHaveBeenCalledWith`.
    - `npx vitest run src/hooks/useGenerateRound.test.ts` passes.
  </acceptance_criteria>
  <done>overtimeMinutes is forwarded as p_overtime_minutes (0 default) with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client hook → PostgREST RPC | useGenerateRound passes admin-supplied params to generate_round |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / Input Validation | useGenerateRound p_overtime_minutes | mitigate | Client merely forwards the value; authoritative bounds + passphrase enforcement live in the SECURITY DEFINER generate_round (Plan 01 T-08-01/02). No client-side trust is granted. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (type contract + all factories consistent).
- `npx vitest run` full suite green.
- generate_round arg assertions pass for both the 20 and omitted cases.
</verification>

<success_criteria>
- RoundTimer.overtime_seconds is a required field consumed downstream by Plans 03/04.
- Full test suite stays green (backward compatibility preserved).
- useGenerateRound forwards p_overtime_minutes correctly.
</success_criteria>

<output>
Create `.planning/phases/08-timer-migration-core-engine/08-02-SUMMARY.md` when done.
</output>
