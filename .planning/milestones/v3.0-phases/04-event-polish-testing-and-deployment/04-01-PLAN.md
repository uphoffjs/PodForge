---
phase: 04-event-polish-testing-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EventInfoBar.tsx
  - src/components/EventInfoBar.test.tsx
  - src/pages/EventPage.tsx
  - src/pages/EventPage.test.tsx
autonomous: true
requirements:
  - EVNT-05

must_haves:
  truths:
    - "Event page displays info bar with event name prominently"
    - "Info bar shows expandable QR code that toggles on click"
    - "Info bar shows shareable link with a working copy button"
    - "Info bar shows the active player count (not dropped players)"
    - "Info bar shows the current round number (or 'No rounds yet')"
  artifacts:
    - path: "src/components/EventInfoBar.tsx"
      provides: "Event info bar with QR, share, player count, round number"
      min_lines: 60
    - path: "src/components/EventInfoBar.test.tsx"
      provides: "Unit tests for EventInfoBar component"
      min_lines: 50
  key_links:
    - from: "src/components/EventInfoBar.tsx"
      to: "src/components/QRCodeDisplay.tsx"
      via: "imports QRCodeDisplay for expandable QR"
      pattern: "import.*QRCodeDisplay"
    - from: "src/pages/EventPage.tsx"
      to: "src/components/EventInfoBar.tsx"
      via: "renders EventInfoBar in place of separate header and share sections"
      pattern: "import.*EventInfoBar"
---

<objective>
Build the event info bar component (EVNT-05) that consolidates event metadata into a compact, always-visible bar at the top of the event page. The bar displays: event name, expandable QR code, shareable link with copy button, active player count, and current round number.

Purpose: Players and admins need a quick glance at event status. Currently the event name is at the top and the share section is buried at the bottom. The info bar consolidates everything into one compact area.

Output: EventInfoBar component with tests, integrated into EventPage.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/EventPage.tsx
@src/components/QRCodeDisplay.tsx
@src/types/database.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/database.ts:
```typescript
export type Event = {
  id: string
  name: string
  status: 'active' | 'ended'
  created_at: string
}

export type Player = {
  id: string
  event_id: string
  name: string
  status: 'active' | 'dropped'
  created_at: string
}

export type Round = {
  id: string
  event_id: string
  round_number: number
  created_at: string
}
```

From src/components/QRCodeDisplay.tsx:
```typescript
type QRCodeDisplayProps = {
  eventId: string
}
export function QRCodeDisplay({ eventId }: QRCodeDisplayProps)
```

EventPage currently has separate sections:
- Lines 209-217: Event header (name + status badge)
- Lines 319-343: Share section (QR code + link + copy button) at the BOTTOM of the page
These will be consolidated into EventInfoBar at the top.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventInfoBar component with tests</name>
  <files>
    src/components/EventInfoBar.tsx
    src/components/EventInfoBar.test.tsx
  </files>
  <action>
Create `src/components/EventInfoBar.tsx` with the following specification:

**Props interface:**
```typescript
type EventInfoBarProps = {
  eventId: string
  eventName: string
  eventStatus: 'active' | 'ended'
  activePlayerCount: number
  currentRoundNumber: number | null
}
```

**Layout (mobile-first, dark theme):**
- Compact card with `bg-surface-raised border border-border rounded-xl p-4` styling
- `w-full max-w-lg` to match other cards on EventPage
- Event name as the heading (text-accent, font-display, centered, data-testid="event-info-name")
- Status badge below name (same styling as current event-status badge)
- Stats row with 2 items side by side: "Players: {count}" and "Round: {number}" (or "No rounds yet" if null). Use data-testid="event-info-player-count" and data-testid="event-info-round-number"
- QR code section: collapsed by default. Toggle button labeled "Show QR Code" / "Hide QR Code" with ChevronDown/ChevronUp icon from lucide-react. When expanded, render QRCodeDisplay component. Use data-testid="event-info-qr-toggle" for the button and data-testid="event-info-qr-section" for the expandable wrapper
- Share link row: read-only input with event URL + Copy button (reuse the Copy icon from lucide-react). Use data-testid="event-info-share-link" and data-testid="event-info-copy-btn". Copy uses navigator.clipboard.writeText with toast.success/error from sonner

**Important:** Import QRCodeDisplay from '@/components/QRCodeDisplay' for the QR code rendering. Use `${window.location.origin}/event/${eventId}` for the share link (same as current EventPage).

**Create tests** in `src/components/EventInfoBar.test.tsx`:
- Renders event name
- Renders active player count
- Renders round number when provided
- Renders "No rounds yet" when currentRoundNumber is null
- QR code is collapsed by default (QRCodeDisplay not rendered)
- Clicking QR toggle expands/collapses the QR section
- Copy button calls navigator.clipboard.writeText with correct URL
- Shows event status badge
- Test data-testid attributes are present

Mock QRCodeDisplay, navigator.clipboard, and sonner toast as needed. Use @testing-library/react with vitest.
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx vitest --run src/components/EventInfoBar.test.tsx</automated>
  </verify>
  <done>EventInfoBar component renders all 5 info elements (name, QR toggle, share link+copy, player count, round number). All unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate EventInfoBar into EventPage, remove redundant sections</name>
  <files>
    src/pages/EventPage.tsx
    src/pages/EventPage.test.tsx
  </files>
  <action>
Modify `src/pages/EventPage.tsx`:

1. Import EventInfoBar from '@/components/EventInfoBar'
2. Compute activePlayerCount: `players?.filter(p => p.status === 'active').length ?? 0`
3. Replace the "Event header" section (lines ~209-217, the h1 with event name + status badge) AND the "Share section" at the bottom (lines ~319-343, the QR code + share link card) with a single EventInfoBar placed right after the loading/error guards, before the event-ended banner:

```tsx
{/* Event info bar */}
<EventInfoBar
  eventId={eventId}
  eventName={event?.name ?? ''}
  eventStatus={event?.status ?? 'active'}
  activePlayerCount={activePlayerCount}
  currentRoundNumber={currentRound?.round_number ?? null}
/>
```

4. Remove the handleCopyLink callback (it's now inside EventInfoBar)
5. Remove the QRCodeDisplay import (no longer used directly in EventPage)
6. Remove the Copy import from lucide-react (no longer used directly in EventPage)
7. Keep all other sections unchanged (join form, timer, admin controls, player list, etc.)

**Update existing EventPage tests** in `src/pages/EventPage.test.tsx`:
- Update any assertions that reference the old header testids (event-name, event-status) to reference the new EventInfoBar testids (event-info-name, etc.) OR mock EventInfoBar to keep tests focused
- Ensure all existing EventPage tests still pass after the refactor
- The share-link-input and share-copy-btn testids are now inside EventInfoBar, so any EventPage tests that reference them should be updated or the EventInfoBar should be left unmocked
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx vitest --run src/pages/EventPage.test.tsx src/components/EventInfoBar.test.tsx</automated>
  </verify>
  <done>EventPage renders EventInfoBar at the top with correct props. Old header and share sections removed. All EventPage tests and EventInfoBar tests pass. No duplicate QR/share UI on the page.</done>
</task>

</tasks>

<verification>
1. `npx vitest --run` — all 346+ tests pass (existing + new EventInfoBar tests)
2. `npx tsc -b` — no TypeScript errors
3. `npm run lint` — no ESLint errors
4. Manual: event page shows info bar with name, expandable QR, share link + copy, player count, round number
</verification>

<success_criteria>
- EventInfoBar component exists and renders all 5 required elements
- QR code is expandable (collapsed by default, toggles on click)
- Share link has working copy button
- Active player count shows only active players (not dropped)
- Round number shows current round or "No rounds yet"
- All tests pass (vitest + tsc + lint)
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-polish-testing-and-deployment/04-01-SUMMARY.md`
</output>
