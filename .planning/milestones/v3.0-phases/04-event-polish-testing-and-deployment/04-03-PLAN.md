---
phase: 04-event-polish-testing-and-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cypress/e2e/timer.cy.js
  - cypress/fixtures/timer.json
  - DEPLOYMENT.md
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "Cypress E2E tests cover the timer user flow (display, countdown, admin controls)"
    - "Integration tests verify major user flows end-to-end (create event, join, generate round, timer, self-drop)"
    - "Deployment instructions exist for Vercel (frontend) and Supabase (backend)"
    - "The app builds successfully for production (vite build passes)"
  artifacts:
    - path: "cypress/e2e/timer.cy.js"
      provides: "E2E tests for timer display and admin controls"
      min_lines: 80
    - path: "cypress/fixtures/timer.json"
      provides: "Timer fixture data for E2E tests"
      min_lines: 5
    - path: "DEPLOYMENT.md"
      provides: "Step-by-step deployment guide for Vercel + Supabase"
      min_lines: 50
  key_links:
    - from: "cypress/e2e/timer.cy.js"
      to: "src/components/TimerDisplay.tsx"
      via: "tests data-testid attributes rendered by TimerDisplay"
      pattern: "timer-display|timer-countdown"
    - from: "DEPLOYMENT.md"
      to: "vite.config.ts"
      via: "references build command and output directory"
      pattern: "vite build|npm run build"
---

<objective>
Add E2E timer tests and create deployment documentation (INFR-05). Timer is the last major feature without Cypress coverage. Deployment instructions document the Vercel + Supabase setup for production.

Purpose: Complete the test coverage story by adding timer E2E tests (the only untested major feature in Cypress). Create deployment docs so the app can be deployed to production.

Output: Timer E2E spec + fixture, deployment documentation, verified production build.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/TimerDisplay.tsx
@src/components/TimerControls.tsx
@src/hooks/useTimer.ts
@src/types/database.ts
@vite.config.ts

<interfaces>
<!-- Key types and data-testid attributes the executor needs -->

From src/types/database.ts:
```typescript
export type RoundTimer = {
  id: string
  round_id: string
  event_id: string
  duration_minutes: number
  status: 'running' | 'paused' | 'cancelled'
  started_at: string
  remaining_seconds: number | null
  paused_at: string | null
  expires_at: string
  created_at: string
}
```

Existing Cypress patterns (from Phase 1.1/2.1):
- cy.getByTestId(id) custom command
- cy.mockEventPage(event, players) for isolated event page testing
- cy.intercept for Supabase REST/RPC mocking
- PostgREST .single() uses content-type: application/vnd.pgrst.object+json
- Spec files use .cy.js extension
- onBeforeLoad for localStorage/sessionStorage setup

Key data-testid attributes on timer components:
- timer-display: main timer container
- timer-countdown: the countdown text
- timer-status: running/paused badge
- timer-controls: admin controls wrapper
- timer-pause-btn, timer-resume-btn, timer-extend-btn, timer-cancel-btn

Supabase project config:
- supabase/migrations/ directory with 3 migrations
- .env contains VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SUPABASE_DB_PASSWORD
- No vercel.json exists (Vite default config works with Vercel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timer E2E tests with Cypress</name>
  <files>
    cypress/e2e/timer.cy.js
    cypress/fixtures/timer.json
  </files>
  <action>
**Create `cypress/fixtures/timer.json`:**
```json
{
  "running": {
    "id": "timer-1",
    "round_id": "round-1",
    "event_id": "test-event-id",
    "duration_minutes": 60,
    "status": "running",
    "started_at": "2026-02-25T12:00:00.000Z",
    "remaining_seconds": null,
    "paused_at": null,
    "expires_at": "2026-02-25T13:00:00.000Z",
    "created_at": "2026-02-25T12:00:00.000Z"
  },
  "paused": {
    "id": "timer-1",
    "round_id": "round-1",
    "event_id": "test-event-id",
    "duration_minutes": 60,
    "status": "paused",
    "started_at": "2026-02-25T12:00:00.000Z",
    "remaining_seconds": 1800,
    "paused_at": "2026-02-25T12:30:00.000Z",
    "expires_at": "2026-02-25T13:00:00.000Z",
    "created_at": "2026-02-25T12:00:00.000Z"
  },
  "cancelled": {
    "id": "timer-1",
    "round_id": "round-1",
    "event_id": "test-event-id",
    "duration_minutes": 60,
    "status": "cancelled",
    "started_at": "2026-02-25T12:00:00.000Z",
    "remaining_seconds": null,
    "paused_at": null,
    "expires_at": "2026-02-25T13:00:00.000Z",
    "created_at": "2026-02-25T12:00:00.000Z"
  }
}
```

**Create `cypress/e2e/timer.cy.js`:**

Follow the established Cypress patterns from this project:
- Use cy.getByTestId() for all element selection
- Use cy.intercept() to mock Supabase REST endpoints
- Use the fixtures for timer data
- Suppress WebSocket/Realtime errors in uncaught:exception handler

**Test cases to implement:**

1. **Timer display - running state:**
   - Set up event page with a running timer (intercept GET /rest/v1/round_timers to return running fixture)
   - Assert timer-display is visible
   - Assert timer-countdown shows time remaining (mm:ss format)
   - Assert timer shows running status indicator

2. **Timer display - paused state:**
   - Set up with paused timer fixture
   - Assert timer shows paused indicator
   - Assert countdown shows remaining time (30:00 for 1800 seconds)

3. **Timer display - cancelled timer not shown:**
   - Set up with cancelled timer fixture
   - Assert timer-display is NOT visible (EventPage conditionally hides cancelled timers)

4. **Timer admin controls - visible for admin:**
   - Set up event page with admin passphrase in sessionStorage + running timer
   - Assert timer-controls section is visible
   - Assert pause/extend/cancel buttons are present

5. **Timer admin controls - hidden for non-admin:**
   - Set up event page WITHOUT admin passphrase + running timer
   - Assert timer-controls section does NOT exist

6. **Timer admin controls - pause action:**
   - Set up as admin with running timer
   - Intercept POST /rest/v1/rpc/pause_timer
   - Click pause button
   - Assert pause RPC was called

7. **Timer admin controls - extend action:**
   - Set up as admin with running timer
   - Intercept POST /rest/v1/rpc/extend_timer
   - Click extend (+5 min) button
   - Assert extend RPC was called

**Important patterns:**
- Use `cy.mockEventPage()` as a base, then add additional intercepts for round_timers
- For admin tests, use onBeforeLoad to set sessionStorage passphrase before page renders
- Timer fixture expires_at should be set dynamically relative to current time if countdown assertions need specific values — OR just assert the element exists without checking exact time value (simpler, since countdown depends on wall clock)
- Mock the Realtime channel responses to prevent WebSocket noise

Minimum 7 test cases covering the user-facing timer behavior.
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx cypress run --spec cypress/e2e/timer.cy.js</automated>
  </verify>
  <done>Timer E2E spec passes all test cases. Timer display, admin controls, and state transitions are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Create deployment documentation and verify production build</name>
  <files>
    DEPLOYMENT.md
  </files>
  <action>
**First, verify the production build works:**
Run `npm run build` to confirm Vite produces a clean production build with no errors.

**Create `DEPLOYMENT.md`** at the project root with the following sections:

```markdown
# Deployment Guide

## Architecture

- **Frontend**: React SPA hosted on Vercel
- **Backend**: Supabase (Postgres database + Realtime + Edge Functions/RPCs)

## Prerequisites

- Node.js 18+ and npm
- Supabase account (https://supabase.com)
- Vercel account (https://vercel.com)
- Supabase CLI (`npm i -g supabase`)

## 1. Supabase Setup

### Create Project
1. Go to https://supabase.com/dashboard and create a new project
2. Note your project URL and anon key from Project Settings > API

### Apply Database Migrations
```bash
# Link to your Supabase project
supabase link --project-ref YOUR_PROJECT_REF

# Push all migrations
supabase db push
```

This applies three migrations:
- `00001_*`: Core schema (events, players tables, RLS policies, RPCs)
- `00002_*`: Rounds, pods, pod_players tables, admin RPCs
- `00003_*`: REPLICA IDENTITY FULL for Realtime filtering

### Enable Realtime
1. Go to Database > Replication in Supabase Dashboard
2. Enable Realtime for tables: `events`, `players`, `rounds`, `pods`, `pod_players`, `round_timers`

### Configure RLS
RLS policies are applied via migrations. Verify they are active:
1. Go to Authentication > Policies
2. Confirm policies exist for all tables

## 2. Vercel Deployment

### Environment Variables
Set these in Vercel project settings (Settings > Environment Variables):

| Variable | Source |
|----------|--------|
| `VITE_SUPABASE_URL` | Supabase Dashboard > Project Settings > API > Project URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase Dashboard > Project Settings > API > anon/public key |

### Deploy via CLI
```bash
# Install Vercel CLI
npm i -g vercel

# Deploy (first time: follow prompts to link project)
vercel

# Production deployment
vercel --prod
```

### Deploy via GitHub Integration
1. Import your repository at https://vercel.com/new
2. Framework preset: Vite
3. Build command: `npm run build`
4. Output directory: `dist`
5. Add environment variables (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
6. Deploy

### SPA Routing
Vercel handles SPA routing automatically for Vite projects. The `/event/:eventId` routes will work without additional configuration.

## 3. Post-Deployment Verification

1. Visit your Vercel URL
2. Create a new event (should redirect to event page)
3. Open the event link on a second device/browser
4. Join as a player — verify real-time player list updates
5. Generate a round — verify pod assignments appear on both devices
6. Start a timer — verify countdown is synchronized

## Environment Variables Reference

| Variable | Required | Description |
|----------|----------|-------------|
| `VITE_SUPABASE_URL` | Yes | Supabase project URL |
| `VITE_SUPABASE_ANON_KEY` | Yes | Supabase anonymous/public API key |
| `SUPABASE_DB_PASSWORD` | Local only | Database password for `supabase db push` (in .env, gitignored) |

## Local Development

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Run tests
npx vitest --run        # Unit tests
npx cypress run          # E2E tests

# Build for production
npm run build
npm run preview          # Preview production build locally
```
```

Adapt the content based on the actual project structure (check if vercel.json is needed, check build output dir, etc.). The key requirement is that the instructions are complete and accurate for this specific project.

After creating the file, run `npm run build` to verify the production build succeeds.
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npm run build</automated>
  </verify>
  <done>DEPLOYMENT.md exists with complete Vercel + Supabase setup instructions. Production build succeeds without errors.</done>
</task>

</tasks>

<verification>
1. `npx cypress run --spec cypress/e2e/timer.cy.js` — all timer E2E tests pass
2. `npm run build` — production build succeeds
3. DEPLOYMENT.md exists with Vercel + Supabase instructions
4. Total Cypress specs: 14 (13 existing + 1 new timer spec)
</verification>

<success_criteria>
- Timer E2E tests cover display states (running, paused, cancelled), admin controls visibility, and admin actions (pause, extend)
- DEPLOYMENT.md has complete, accurate instructions for Vercel + Supabase deployment
- Production build succeeds with zero errors
- All existing tests remain passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-polish-testing-and-deployment/04-03-SUMMARY.md`
</output>
