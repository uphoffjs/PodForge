---
phase: 02-pod-generation-and-admin-controls
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/components/AdminControls.tsx
  - src/components/AdminPassphraseModal.tsx
  - src/components/PodCard.tsx
  - src/components/RoundDisplay.tsx
  - src/pages/EventPage.tsx
autonomous: true
requirements:
  - PODG-01
  - PODG-04
  - PODG-05

must_haves:
  truths:
    - "Admin can click Generate Round and see pod assignments appear"
    - "Passphrase modal appears when admin is not yet authenticated"
    - "Pod cards display player names with seat numbers (1st-4th)"
    - "Bye pod is visually distinct with 'Sitting Out' label and no seat numbers"
    - "Current round pods are displayed prominently above player list"
  artifacts:
    - path: "src/components/AdminControls.tsx"
      provides: "Admin control panel with Generate Round button"
      exports: ["AdminControls"]
    - path: "src/components/AdminPassphraseModal.tsx"
      provides: "Modal for passphrase entry when admin action requires authentication"
      exports: ["AdminPassphraseModal"]
    - path: "src/components/PodCard.tsx"
      provides: "Pod card displaying players with seat numbers, and bye pod variant"
      exports: ["PodCard"]
    - path: "src/components/RoundDisplay.tsx"
      provides: "Current round display with pod cards grid"
      exports: ["RoundDisplay"]
    - path: "src/pages/EventPage.tsx"
      provides: "Updated event page integrating AdminControls and RoundDisplay"
      contains: "AdminControls"
  key_links:
    - from: "src/components/AdminControls.tsx"
      to: "src/hooks/useGenerateRound.ts"
      via: "useGenerateRound mutation on button click"
      pattern: "useGenerateRound"
    - from: "src/components/AdminControls.tsx"
      to: "src/lib/pod-algorithm.ts"
      via: "generatePods called to compute assignments before RPC"
      pattern: "generatePods"
    - from: "src/components/RoundDisplay.tsx"
      to: "src/hooks/usePods.ts"
      via: "usePods hook to fetch pod data for current round"
      pattern: "usePods"
    - from: "src/pages/EventPage.tsx"
      to: "src/components/RoundDisplay.tsx"
      via: "Renders RoundDisplay with current round data"
      pattern: "<RoundDisplay"
---

<objective>
Wire the pod generation algorithm into the admin UI flow and display pod assignments. Admin clicks "Generate Round," enters passphrase if needed, the algorithm runs client-side, results are saved via RPC, and pod cards appear for all connected clients.

Purpose: This is the core user-facing feature -- the moment when the admin generates a round and every player sees their pod assignment. It connects the algorithm (Plan 02) to the database (Plan 01) through the UI.

Output: AdminControls component, AdminPassphraseModal, PodCard component, RoundDisplay component, updated EventPage.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-pod-generation-and-admin-controls/02-01-SUMMARY.md
@.planning/phases/02-pod-generation-and-admin-controls/02-02-SUMMARY.md
@src/pages/EventPage.tsx
@src/hooks/useAdminAuth.ts
@src/components/ConfirmDialog.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminPassphraseModal and AdminControls components</name>
  <files>
    src/components/AdminPassphraseModal.tsx
    src/components/AdminControls.tsx
  </files>
  <action>
**1. Create src/components/AdminPassphraseModal.tsx:**

A modal dialog for passphrase entry. Styled consistently with CreateEventModal (same overlay pattern: fixed inset-0, bg-black/60 backdrop, rounded-2xl card).

Props:
```typescript
interface AdminPassphraseModalProps {
  isOpen: boolean
  onSubmit: (passphrase: string) => void
  onCancel: () => void
  isLoading?: boolean
  error?: string | null
}
```

- Input field for passphrase (type="password"), auto-focused when open.
- Submit button ("Confirm") and Cancel button.
- Display error message below input if `error` prop is set.
- Close on overlay click (same pattern as CreateEventModal: check e.target === e.currentTarget).
- Close on Escape key.
- data-testid attributes: `admin-passphrase-modal`, `admin-passphrase-input`, `admin-passphrase-submit`, `admin-passphrase-cancel`, `admin-passphrase-error`.

**2. Create src/components/AdminControls.tsx:**

Admin control panel shown when `isAdmin` is true. Contains a "Generate Next Round" button.

Props:
```typescript
interface AdminControlsProps {
  eventId: string
  isAdmin: boolean
  passphrase: string | null
  onPassphraseNeeded: () => void
  players: Player[]
  previousRounds: RoundHistory[]  // from pod-algorithm types
  isEventEnded: boolean
}
```

Behavior:
- Show "Generate Next Round" button (prominent, accent color, with lucide `Shuffle` icon).
- If `isEventEnded`, disable the button with tooltip "Event has ended".
- On click:
  1. If `passphrase` is null, call `onPassphraseNeeded()` to trigger the passphrase modal.
  2. Otherwise, call `generatePods(activePlayers, previousRounds)` from pod-algorithm.
  3. If algorithm throws (< 4 players), show error toast with the message.
  4. If algorithm returns warnings, show warning toasts.
  5. Call `useGenerateRound` mutation with passphrase and pod assignments.
  6. On mutation error (invalid passphrase), show error toast.
- Use the established pattern: `useGenerateRound(eventId)` hook.
- Display current round number (from rounds data) as context: "Round {N}" or "No rounds yet".
- data-testid: `admin-controls`, `generate-round-btn`.

Styling: Card-style container (bg-surface-raised border border-border rounded-xl p-4), consistent with existing cards. Button uses bg-accent text-surface with min-h-[44px] for touch target.

Import `generatePods` from `@/lib/pod-algorithm`, `useGenerateRound` from `@/hooks/useGenerateRound`, `useRounds` from `@/hooks/useRounds`, `useEventPlayers` from `@/hooks/useEventPlayers`. Transform player/round data into the format expected by generatePods (build RoundHistory from fetched rounds+pods data).

Note: The AdminControls needs to build RoundHistory from the fetched round/pod data. Create a helper function `buildRoundHistoryFromData` that takes the rounds and their pods and transforms them into the `RoundHistory[]` format the algorithm expects. This helper should live in AdminControls or be exported from pod-algorithm.ts.
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Verify AdminPassphraseModal renders with form, AdminControls renders with Generate Round button</manual>
  </verify>
  <done>AdminPassphraseModal renders passphrase input with submit/cancel, AdminControls renders Generate Round button that calls pod algorithm and useGenerateRound mutation</done>
</task>

<task type="auto">
  <name>Task 2: Create PodCard, RoundDisplay, and integrate into EventPage</name>
  <files>
    src/components/PodCard.tsx
    src/components/RoundDisplay.tsx
    src/pages/EventPage.tsx
  </files>
  <action>
**1. Create src/components/PodCard.tsx:**

Displays a single pod's players with seat numbers. The most important UI element -- must be glanceable at arm's length.

Props:
```typescript
interface PodCardProps {
  podNumber: number
  isBye: boolean
  players: { playerId: string, playerName: string, seatNumber: number | null }[]
  currentPlayerId: string | null  // highlight "you"
}
```

**Non-bye pod:**
- Card with colored left border (cycle through accent colors: pod 1 = blue #3b82f6, pod 2 = green #10b981, pod 3 = amber #f59e0b, pod 4 = red #ef4444, then repeat for pods 5+).
- Header: "Pod {N}" in bold.
- Player list: each row shows seat number badge (1st, 2nd, 3rd, 4th) + player name. If player is current user (playerId === currentPlayerId), highlight with accent color background and "(You)" suffix.
- Large player names (text-lg font-semibold) for glanceability.
- Seat badges: small rounded pill with ordinal (1st, 2nd, 3rd, 4th).
- data-testid: `pod-card-{podNumber}`, `pod-player-{playerId}`, `pod-seat-{seatNumber}`.

**Bye pod (isBye = true):**
- Muted card: bg-surface border border-border/50, no colored border.
- Header: "Sitting Out" (not "Pod N") with a muted icon (lucide `Coffee` or `Clock`).
- Player names listed without seat numbers, in muted text (text-text-secondary).
- data-testid: `pod-card-bye`.

**2. Create src/components/RoundDisplay.tsx:**

Displays the current round's pods.

Props:
```typescript
interface RoundDisplayProps {
  roundId: string
  roundNumber: number
  currentPlayerId: string | null
}
```

Behavior:
- Uses `usePods(roundId)` to fetch pods for this round.
- Loading state: spinner while pods load.
- Renders pod cards in a responsive grid: 1 column on mobile, 2 columns on md+ screens.
- Non-bye pods first, bye pod last (if any).
- Header: "Round {N}" with round number.
- data-testid: `round-display`, `round-number`.

**3. Update src/pages/EventPage.tsx:**

Add current round display and admin controls to the event page.

Changes:
- Import `useCurrentRound` and `useRounds` hooks.
- Import `RoundDisplay` and `AdminControls` and `AdminPassphraseModal` components.
- Add state: `showPassphraseModal` (boolean), `pendingAdminAction` (to track what triggered the modal).
- Fetch current round with `useCurrentRound(eventId)`.
- Fetch all rounds with `useRounds(eventId)`.

Layout order (top to bottom):
1. Event header (existing)
2. Join form (existing, if not joined)
3. **Current round display** (NEW -- if current round exists, show RoundDisplay)
4. **Admin controls** (NEW -- if isAdmin and event is active, show AdminControls)
5. Player list (existing)
6. Admin: Add Player (existing)
7. Leave Event button (existing)
8. Share section (existing)

AdminPassphraseModal integration:
- When AdminControls calls `onPassphraseNeeded`, set `showPassphraseModal = true`.
- When modal submits, store passphrase via `useAdminAuth.setPassphrase()`, close modal, and re-trigger the pending action.
- Handle invalid passphrase: on RPC error, show error in modal, clear stored passphrase.

If event status is 'ended', show a banner at the top: "This event has ended" (bg-surface-raised text-text-secondary, with lucide `Lock` icon). Hide join form, hide admin controls, hide Leave Event button. Pods and player list remain visible (read-only historical view).

data-testid: `event-ended-banner`.
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx tsc --noEmit 2>&1 | tail -20</automated>
    <manual>Verify EventPage compiles with RoundDisplay and AdminControls integrated, PodCard renders with seat numbers</manual>
  </verify>
  <done>PodCard displays players with seat numbers and bye variant, RoundDisplay shows current round pods, EventPage integrates all new components in correct layout order, ended events show read-only view</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. `npx vitest run` -- all existing tests pass (no regressions)
3. AdminControls renders Generate Round button and calls pod algorithm + RPC
4. PodCard displays seat numbers for non-bye pods and muted style for bye pods
5. RoundDisplay fetches and renders pods for the current round
6. EventPage integrates new components in correct layout order
7. Ended events show read-only banner with historical data visible
</verification>

<success_criteria>
- Admin can click Generate Round, enter passphrase if needed, and see pod assignments
- Pod cards show player names with seat ordinals (1st-4th), current player highlighted
- Bye pod shows "Sitting Out" label with muted styling, no seat numbers
- Event page shows current round prominently above player list
- Ended events are read-only with banner, no admin controls
- Passphrase modal follows existing CreateEventModal styling patterns
- All components use data-testid attributes for future E2E testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-pod-generation-and-admin-controls/02-03-SUMMARY.md`
</output>
