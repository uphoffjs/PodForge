---
phase: 01.2-audit-bug-fixes-production-and-test-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ['01', '02']
files_modified: []
autonomous: true
requirements:
  - INFR-04
gap_closure: true

must_haves:
  truths:
    - "All Cypress E2E tests pass after Plan 01 and Plan 02 fixes are applied"
  artifacts: []
  key_links: []
---

<objective>
Run the full Cypress E2E suite as a verification gate after both Plan 01 (test infrastructure fixes) and Plan 02 (production code fixes) are complete.

Purpose: Plans 01 and 02 run in parallel (Wave 1) and both modify Cypress test infrastructure and production code that affect each other. Neither plan can run the full E2E suite alone because mockEventPage format changes (Plan 01) affect tests that also depend on testid renames (Plan 02). This Wave 2 checkpoint ensures all fixes integrate correctly.

Output: Passing E2E suite confirming all 24+ tests work with the combined fixes.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-audit-bug-fixes-production-and-test-infrastructure/01.2-01-SUMMARY.md
@.planning/phases/01.2-audit-bug-fixes-production-and-test-infrastructure/01.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Cypress E2E suite and confirm all tests pass</name>
  <files></files>
  <action>
Run the full Cypress E2E test suite to verify that all fixes from Plan 01 and Plan 02 integrate correctly:

```bash
npx cypress run
```

All 24+ tests must pass. If any tests fail:
1. Read the failure output carefully
2. Determine whether the failure is from an incomplete fix in Plan 01 or Plan 02
3. Apply the targeted fix (do not re-architect — the plans specify the correct approach)
4. Re-run the failing spec in isolation: `npx cypress run --spec cypress/e2e/{failing-spec}.cy.js`
5. Once isolated fix passes, re-run the full suite to confirm no regressions

Common failure modes to watch for:
- `[event]` array wrapping still present in a spec not covered by Plan 01 — fix the intercept to return plain object with pgrst content-type header
- `join-error` testid still referenced in a spec not updated by Plan 02 — update to `join-mutation-error` or `join-validation-error` as appropriate
- Visual regression baseline mismatch — expected after fixing the localStorage format; update baselines with `npx cypress run --env updateSnapshots=true --spec cypress/e2e/visual-regression.cy.js`
  </action>
  <verify>
    <automated>cd /Users/jacobstoragepug/Desktop/PodForge && npx cypress run 2>&1 | tail -20</automated>
    <manual>Verify the Cypress output shows all specs passing with 0 failures.</manual>
  </verify>
  <done>Full Cypress E2E suite passes with 0 failures, confirming all Plan 01 and Plan 02 fixes integrate correctly.</done>
</task>

</tasks>

<verification>
`npx cypress run` exits with code 0 and reports 0 failures across all spec files.
</verification>

<success_criteria>
- All Cypress E2E specs pass (0 failures)
- No skipped tests (unless pre-existing and documented)
- Visual regression baselines updated if needed after localStorage format fix
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-audit-bug-fixes-production-and-test-infrastructure/01.2-03-SUMMARY.md`
</output>
