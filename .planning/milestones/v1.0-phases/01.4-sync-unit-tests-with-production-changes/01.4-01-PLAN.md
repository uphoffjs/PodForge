---
phase: 01.4-sync-unit-tests-with-production-changes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useJoinEvent.test.ts
  - src/components/JoinEventForm.test.tsx
autonomous: true
gap_closure: true
requirements:
  - INFR-04
  - EVNT-02
  - PLYR-05

must_haves:
  truths:
    - "Full unit test suite passes 226/226 with zero failures"
    - "useJoinEvent hook test correctly verifies storePlayerId is NOT called by the hook"
    - "JoinEventForm validation error tests query join-validation-error testid"
    - "JoinEventForm mutation error tests query join-mutation-error testid"
    - "Full Cypress E2E suite still passes 44/44 with no regressions"
  artifacts:
    - path: "src/hooks/useJoinEvent.test.ts"
      provides: "Updated hook test verifying storePlayerId is not called"
      contains: "not.toHaveBeenCalled"
    - path: "src/components/JoinEventForm.test.tsx"
      provides: "Updated component tests with correct split testids"
      contains: "join-validation-error"
  key_links:
    - from: "src/components/JoinEventForm.test.tsx"
      to: "src/components/JoinEventForm.tsx"
      via: "data-testid queries matching production testids"
      pattern: "join-validation-error|join-mutation-error"
    - from: "src/hooks/useJoinEvent.test.ts"
      to: "src/hooks/useJoinEvent.ts"
      via: "mock assertions matching hook behavior"
      pattern: "mockStorePlayerId.*not.*toHaveBeenCalled"
---

<objective>
Fix 8 failing unit tests that were invalidated by Phase 1.2 production code changes: update the stale `storePlayerId` assertion in `useJoinEvent.test.ts` and replace all stale `join-error` testid queries in `JoinEventForm.test.tsx` with the correct `join-validation-error` or `join-mutation-error` testids.

Purpose: Restore the unit test suite to 226/226 passing, closing the UNIT-TEST-SYNC gap from the v1.0 milestone audit. No production code changes are needed.
Output: Two updated test files, full green unit suite, full green E2E suite.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-sync-unit-tests-with-production-changes/01.4-RESEARCH.md

# Production source files (read-only reference for correct behavior)
@src/hooks/useJoinEvent.ts
@src/components/JoinEventForm.tsx
@src/pages/EventPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale unit test assertions in useJoinEvent.test.ts and JoinEventForm.test.tsx</name>
  <files>src/hooks/useJoinEvent.test.ts, src/components/JoinEventForm.test.tsx</files>
  <action>
**File 1: `src/hooks/useJoinEvent.test.ts`** — Fix 1 failing test (line 78-91)

The test "calls storePlayerId with eventId and player.id on success" asserts `expect(mockStorePlayerId).toHaveBeenCalledWith('evt1', 'player-uuid-123')`. This fails because Phase 1.2 moved `storePlayerId` out of the hook's `onSuccess` into `EventPage.handleJoined`.

Fix: Rename the test to "does not call storePlayerId (caller responsibility)" and change the assertion to `expect(mockStorePlayerId).not.toHaveBeenCalled()`. Keep the mock setup and mutation trigger — the test should still verify the hook completes successfully (`isSuccess === true`) and that `storePlayerId` is NOT invoked by the hook. Do NOT remove the `vi.mock('@/lib/player-identity')` — it must stay to prevent import side effects.

**File 2: `src/components/JoinEventForm.test.tsx`** — Fix 7 failing tests by replacing stale `join-error` testid

Apply these exact replacements based on which error path each test exercises:

**Validation error tests (use `join-validation-error`):**
- Line 48: `screen.getByTestId('join-error')` → `screen.getByTestId('join-validation-error')`
- Line 60: `screen.getByTestId('join-error')` → `screen.getByTestId('join-validation-error')`
- Line 73: `screen.getByTestId('join-error')` → `screen.getByTestId('join-validation-error')`
- Line 86: `screen.queryByTestId('join-error')` → `screen.queryByTestId('join-validation-error')`

**Mutation error tests (use `join-mutation-error`):**
- Line 121: `screen.getByTestId('join-error')` → `screen.getByTestId('join-mutation-error')`
- Line 132: `screen.getByTestId('join-error')` → `screen.getByTestId('join-mutation-error')`
- Line 143: `screen.getByTestId('join-error')` → `screen.getByTestId('join-mutation-error')`

**Mixed test — clears validation error on resubmit (use `join-validation-error`):**
- Line 171: `screen.getByTestId('join-error')` → `screen.getByTestId('join-validation-error')`
- Line 180: `screen.queryByTestId('join-error')` → `screen.queryByTestId('join-validation-error')`

IMPORTANT: Do NOT blindly find-and-replace all `join-error` with one testid. The validation tests (client-side: short name, empty, whitespace) use `join-validation-error`. The mutation tests (server-side: duplicate name code 23505, generic server error, no code property) use `join-mutation-error`. The mixed test (clears validation error) uses `join-validation-error` for both the presence and absence checks.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useJoinEvent.test.ts src/components/JoinEventForm.test.tsx</automated>
    <manual>All 8 previously-failing tests now pass with correct testid queries</manual>
  </verify>
  <done>All tests in both files pass. useJoinEvent test correctly asserts storePlayerId is NOT called. JoinEventForm validation tests use join-validation-error, mutation tests use join-mutation-error.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suites pass with no regressions</name>
  <files>src/hooks/useJoinEvent.test.ts, src/components/JoinEventForm.test.tsx</files>
  <action>
Run the full unit test suite to confirm 226/226 pass:

```bash
npx vitest run
```

Expected: 226 tests passing, 0 failures. If any tests fail that are NOT the 8 being fixed, investigate — the fix may have introduced an unintended side effect.

Then run the full Cypress E2E suite to confirm no regressions:

```bash
npx cypress run
```

Expected: 44/44 tests passing. The unit test changes should have zero impact on E2E tests (they are separate test files), but confirm to satisfy Success Criteria 4.

Finally, run Stryker mutation testing scoped to the two production files these tests cover:

```bash
npx stryker run --mutate "src/hooks/useJoinEvent.ts" --mutate "src/components/JoinEventForm.tsx"
```

Expected: Mutation score >= 80%. If surviving mutants are found in critical paths (duplicate name error handling, validation logic), strengthen the test assertions. Document the mutation score in the SUMMARY.

If Stryker mutation testing takes longer than 5 minutes or encounters configuration issues, skip it and note in the SUMMARY that mutation testing was deferred. The primary gate is 226/226 unit + 44/44 E2E.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
    <manual>Full unit suite shows 226 passing, 0 failing. E2E suite shows 44 passing.</manual>
  </verify>
  <done>226/226 unit tests pass. 44/44 E2E tests pass. Mutation score documented. No regressions introduced.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` → 226 tests, 0 failures
2. `npx vitest run src/hooks/useJoinEvent.test.ts src/components/JoinEventForm.test.tsx` → all tests in both files pass
3. `npx cypress run` → 44/44 passing
4. `useJoinEvent.test.ts` contains assertion that storePlayerId is NOT called
5. `JoinEventForm.test.tsx` contains no references to stale `join-error` testid
6. `JoinEventForm.test.tsx` uses `join-validation-error` for validation tests and `join-mutation-error` for mutation error tests
</verification>

<success_criteria>
- 226/226 unit tests pass (zero failures)
- 44/44 Cypress E2E tests pass (no regressions)
- No stale `join-error` testid references remain in test files
- storePlayerId assertion updated to verify hook does NOT call it
- Mutation score >= 80% on targeted files (or documented deferral)
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-sync-unit-tests-with-production-changes/01.4-01-SUMMARY.md`
</output>
