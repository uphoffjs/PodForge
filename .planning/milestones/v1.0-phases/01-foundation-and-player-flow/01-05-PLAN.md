---
phase: 01-foundation-and-player-flow
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-04
files_modified: []
autonomous: false
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - PLYR-01
  - PLYR-02
  - PLYR-05
  - INFR-01
  - INFR-02
  - INFR-03

must_haves:
  truths:
    - "Full event lifecycle works: create event -> join players -> see real-time updates -> self-drop"
    - "Mobile-first dark theme is readable and feels MTG-themed"
    - "QR code scans correctly on a phone camera"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete Phase 1 player flow. A human verifies that event creation, player joining, real-time updates, self-drop, QR sharing, and the dark theme all work correctly together on a mobile device (or mobile emulation).

Purpose: Automated tests can verify code correctness but not the feel of the experience -- is the dark theme readable? Do real-time updates feel instant? Is the join flow intuitive? This checkpoint catches integration issues and UX gaps before moving to Phase 2.

Output: Verified Phase 1 delivery or a list of issues to fix.
</objective>

<execution_context>
@/Users/jacobstoragepug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobstoragepug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-player-flow/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete Phase 1 player flow end-to-end</name>
  <files>No files modified -- verification only</files>
  <action>Human verifies the complete Phase 1 player flow works end-to-end. See verification steps below.</action>
  <verify>All 8 verification flows pass without issues.</verify>
  <done>Phase 1 player flow verified: create event, join players, real-time updates, self-drop, QR sharing, dark theme.</done>
  <what-built>
    Complete Phase 1: Foundation and Player Flow. The app supports:
    - Event creation with name and admin passphrase (single-step modal)
    - Player joining via event URL with name input
    - Real-time player list updates across all connected clients
    - Player self-drop with confirmation
    - QR code for event sharing
    - Mobile-first MTG-themed dark UI
    - localStorage player persistence
    - sessionStorage admin passphrase persistence
  </what-built>
  <how-to-verify>
    **Prerequisites:** Ensure the Supabase project is set up with the migration applied and `.env` configured with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`. Run `npm run dev`.

    **Flow 1: Event Creation**
    1. Open http://localhost:5173 on your phone (or mobile emulation in DevTools)
    2. Verify: Landing page shows "Commander Pod Pairer" in a stylized font, dark purple/blue theme
    3. Tap "Create Event" -- modal opens
    4. Enter event name (e.g., "Friday Night Commander") and a passphrase
    5. Tap "Create Event" in the modal
    6. Verify: Navigated to event page, toast shows "Event created!"
    7. Verify: Join form is visible ("Enter your name to join")

    **Flow 2: Player Joining**
    8. Enter your name (e.g., "Jacob") and tap "Join Event"
    9. Verify: Your name appears in the player list, highlighted with a distinct color
    10. Verify: Header shows "Players (1 active, 0 dropped)"

    **Flow 3: Real-Time Updates**
    11. Open a second browser tab/window to the same event URL
    12. In tab 2, enter a different name and join
    13. Verify: Tab 1 shows the new player appear WITHOUT refreshing (within 1-2 seconds)
    14. Verify: Brief flash/highlight animation on the new player entry
    15. Verify: Header updates to "Players (2 active, 0 dropped)"

    **Flow 4: Duplicate Name Prevention**
    16. Open a third tab and try to join with the same name as an existing player
    17. Verify: Error message says "That name is already taken. Try another!" (not a generic error)

    **Flow 5: Self-Drop**
    18. In tab 2, tap "Leave Event" button (should be below the player list, not inline)
    19. Verify: Confirmation dialog appears ("Leave Event?" with warning text)
    20. Tap "Leave Event" to confirm
    21. Verify: Tab 2 shows the join form again
    22. Verify: Tab 1 sees the player move to "Dropped" section in real time
    23. Verify: "Dropped (1)" appears, collapsed by default. Tap to expand and verify grayed-out name.

    **Flow 6: QR Code**
    24. On the event page, find the QR code
    25. Scan it with your phone's native camera app
    26. Verify: Opens the correct event URL in the browser

    **Flow 7: Persistence**
    27. Close and reopen tab 1 (same event URL)
    28. Verify: Auto-recognized as the player (no join form shown)

    **Flow 8: Visual / Theme**
    29. Overall feel: Is the dark theme readable? MTG-inspired purple/blue tones?
    30. Typography: Cinzel headers and Inter body text rendering correctly?
    31. Mobile usability: Touch targets large enough? Text readable at arm's length?
    32. No slow animations: Everything feels snappy?
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 8 flows above pass without issues.
</verification>

<success_criteria>
- Complete event lifecycle verified: create -> join -> real-time -> drop
- Dark MTG theme is visually appropriate and readable
- Mobile-first experience works on phone-sized screens
- Real-time updates are perceptibly instant (< 2 seconds)
- QR code scans correctly
- Persistence (localStorage and sessionStorage) works across page refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-player-flow/01-05-SUMMARY.md`
</output>
